# SIGNAL 계획 2/4 — 인터랙티브 데모 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 방문자가 노선·등급·출발일을 고르면, 기준일(2026-09-22)에 샀을 때의 예측가, 80% 예측 구간, 추천(BUY NOW / DROP EXPECTED / WAIT)과 이유를 보여 주는 데모 섹션을 만든다. 모델 서버 없이 미리 계산한 JSON으로 동작한다. 같은 예측 구간을 케이스 스터디 3-5 챕터의 3D 지형 위에 띠로 얹는다.

**Architecture:** Python 스크립트 `scripts/export_demo.py`가 항공권 저장소의 `v2_predictor.pkl`과 `recommend_action()`을 6개 노선 × LCC/FSC × 출발일 D+3~90에 돌린다. 결과는 `public/data/demo.<기준일>.json`(막대·결과·이유 코드), `public/data/band.<기준일>.json`(3D 띠용 노선·등급 평균 대비 %)이고, `data/facts.json`의 `demoDefault`도 채운다. 사이트는 `ForecastSource` 인터페이스(`src/demo/types.ts`)만 보는 클라이언트 컴포넌트 `DemoApp`이 조작 화면을 그린다. 섹션이 화면 가까이 오면 zod 검사가 든 `StaticForecastSource`(`src/demo/source.ts`)를 dynamic import해 JSON을 받는다. 이유 문장은 JSON에 코드와 값으로만 있고, 언어별 문구 틀(`content/*.json`의 `demo.why.*`)에 `{v.…}` 자리표시로 채운다. 3D는 `band` JSON을 새 점 레이어(kind 4)로 그려 3-5 장면에서만 켠다.

**Tech Stack:** Next.js 16.3 (static export), React 19, zod 4, Vitest 5, Playwright 1.63 + axe, three 0.186 / @react-three/fiber 9.8, Python 3.11 (pandas, workalendar, 항공권 저장소 `V2Predictor`, pytest)

**Spec:** `docs/superpowers/specs/2026-09-23-portfolio-design.md` (§3 3-5 띠 · §6 데모 · §8.1 로딩 순서 · §9 접근성 · §11 수치 관리 · §13 테스트)

**선행 계획:** 계획 1(기반)과 계획 3(3D 지형)이 main에 병합되어 있다(계획 3은 2026-09-24 병합, CI 통과).

## Global Constraints

- 색: 배경 `#070B16`, 그라데이션 `#13203A`, 데이터 점 `#8FB8FF`, 텍스트 `#EEF3FF`, 호박색 `#FFB547`. 새 색을 만들지 않는다(반투명 변형만 허용)
- 이징은 `cubic-bezier(.16,1,.3,1)`(`--ease`) 하나. 플립 글자판·텍스트 리빌·Lenis는 **계획 4**에서 한다. 이 계획에서 예측가는 완성된 값만 보여 주고 `data-flip` 속성으로 자리만 남긴다
- **문구에 숫자 직접 기입 금지**(3개 언어, `tests/unit/content.test.ts`가 강제). facts 수치는 `{model.…}`, 데모 실행 중 값은 `{v.…}` 자리표시로 넣는다. `{v.…}`에 쓰는 이름은 `src/demo/reason.ts`의 `SAMPLE_VALUES`에 있어야 한다
- 데모 JSON에는 **이유 문장 대신 코드+값**을 저장한다(`recommend_action()`의 한국어 `message`를 쓰지 않는다)
- 조합: 노선 `ICN_NRT, NRT_ICN, ICN_KIX, KIX_ICN, ICN_HND, HND_ICN` × 등급 `LCC, FSC` × 출발일 기준일+3 ~ 기준일+90(88일, 모델 `MAX_DTD = 90`)
- 대표 편: 기준일까지 최근 21일 동안 50행 이상 관측된 편(항공사, 출발 시각, 경유 수) 중 가장 많이 관측된 편. 없으면 그 조합은 `null`
- `demo.json` gzip ≤ 500KB, `band.json` gzip ≤ 50KB. 파일 이름에 기준일, 사이트는 `facts.dataVersion`으로 찾는다
- `demo.json`은 데모 섹션이 화면 600px 앞까지 올 때 불러온다(IntersectionObserver). zod가 든 `src/demo/source.ts`는 초기 JS 청크에 들어가면 안 된다
- 접근성: 출발일 막대는 `role="slider"`(←/→/↑/↓, Home/End, 예측 없는 날은 건너뜀), `aria-valuetext`에 날짜·예측가·공휴일. 결과 변경은 **사용자가 조작한 뒤에만**, 조작이 멈추고 700ms 뒤 `aria-live="polite"`로 한 번 알린다. 공휴일은 색 + ◆ 표식 + 이름표, 추천은 글자 배지
- 데모는 움직임 줄이기·3D 꺼짐 상태에서도 그대로 동작해야 한다. JS가 꺼져 있으면 제목·설명·기준일과 "JavaScript를 켜면 동작" 안내가 보인다
- 원본 CSV와 개별 행은 공개하지 않는다. 데모에는 예측값만, 띠는 노선·등급 평균 대비 %만 담는다
- **코드에 한국어 주석**: 파일 머리에 역할 한두 줄, 이유가 안 보이는 로직에 "왜"(사용자 요청). 코드를 그대로 옮겨 적는 주석은 쓰지 않는다
- 커밋 작성자는 noreply(`55799748+hyde0395@users.noreply.github.com`). Task 1에서 확인한다. 평문 이메일을 어디에도 쓰지 않는다
- 사용자와의 대화는 한국어

## Review Focus

1. **데모 데이터를 못 받는 방문자**(네트워크 오류·404): 빈 칸이 아니라 안내 + "다시 시도" 버튼이 보이고, 다시 시도하면 불러와야 한다 → Task 4 단위 테스트(실패 뒤 재요청) + Task 11 e2e(요청 차단 후 해제)
2. **키보드·스크린리더 사용자**: 막대를 키보드만으로 조작하고, 읽는 값에 날짜·가격·공휴일이 들어 있어야 한다 → Task 5 단위 테스트 + Task 11 e2e(키보드, aria-live, axe)
3. **초기 로딩 무게**: zod가 든 데모 데이터 모듈이 초기 청크에 없어야 한다(계획 4의 초기 JS 150KB 목표) → Task 11 빌드 산출물 검사
4. **수치 일관성**: 이유 문장이 모델 규칙과 어긋나면 안 된다. `encode_reco`가 모델의 분기 순서를 재현하고, action이 다르면 스크립트가 멈춘다 → Task 2 pytest
5. **문구 규칙**: 세 언어 키 일치, 숫자 직접 기입 금지, `{v.…}` 해석, 공휴일 이름표 누락 없음 → Task 7 단위 테스트

---

## File Structure

```
scripts/
  py.sh                       (신규) 데이터 스크립트용 파이썬 고르기(iCloud 밖 venv 우선)
  export_demo.py              (신규) demo.<기준일>.json, band.<기준일>.json 생성 + facts.demoDefault
  tests/test_export_demo.py   (신규)
public/data/demo.2026-09-22.json, public/data/band.2026-09-22.json   (신규, 커밋)
data/facts.json               demoDefault 추가
src/demo/
  types.ts                    (신규) 노선·등급·추천 타입, ForecastSource 인터페이스, demoUrl (zod 없음)
  source.ts                   (신규) demoSchema(zod) + StaticForecastSource (dynamic import 전용)
  strip.ts                    (신규) 막대 순수 계산(고를 수 있는 날 오가기, 위치→번호, 높이)
  reason.ts                   (신규) 이유 코드 → 문장, 확신도 문장, SAMPLE_VALUES
src/lib/i18n.ts               fixed1·md 형식, prefill(facts만 먼저 채우기)
src/lib/content.ts            DemoTexts 타입, demoTexts(locale)
src/lib/facts.ts              demoDefault 스키마
src/components/sections/Demo.tsx   (신규) 서버 섹션(제목·설명·noscript) + DemoApp
src/components/demo/
  DemoApp.tsx                 (신규, 'use client') 질문 문장·상태·불러오기·알림
  DateStrip.tsx               (신규) 출발일 막대(slider)
  DemoResult.tsx              (신규) 예측가·구간 띠·배지·이유
src/components/HomePage.tsx   Demo 섹션 끼우기
src/components/sections/CaseStudy.tsx   interval 챕터 문단 3개로
src/styles/globals.css        데모 스타일, #demo를 3D 카드 규칙에 추가, .sr-only
content/{ko,en,ja}.json       demo 블록, case.interval.body3, figure.interval 수정
src/three/data.ts             bandSchema, dateIndex, daysBetween, kind 4 띠 점, loadSceneData가 band도 받음
src/three/shaders.ts          kind 4(uBand)
src/three/scenes.ts           'demo' 장면, band 필드(3-5에서만 1)
src/three/activeScene.ts      'demo' 섹션 키
src/three/TerrainPoints.tsx   uBand
src/three/TerrainScene.tsx    band 데이터 전달
public/fallback/interval.webp 다시 캡처
tests/unit/demo-source.test.ts, demo-strip.test.ts, demo-reason.test.ts   (신규)
tests/unit/i18n.test.ts, content.test.ts, facts.test.ts, three-data.test.ts, three-scenes.test.ts   수정
tests/e2e/demo.spec.ts        (신규)
tests/e2e/site.spec.ts        섹션 목록에 demo, JS 없이 안내
package.json                  demo·pytest 스크립트, 데이터 스크립트가 py.sh를 거치게
README.md, CLAUDE.md, 스펙 §5.3·§6         문서 갱신
```

---

### Task 1: 작업 환경 — 브랜치, 커밋 이메일, 파이썬 선택기

**Files:**
- Create: `scripts/py.sh`
- Modify: `package.json` (scripts)
- Modify: `CLAUDE.md` (진행 상태)

**배경:** 항공권 저장소의 `.venv`는 iCloud로 기기 간에 `lib`만 넘어와 이 맥북에서 깨져 있다(`.venv/bin/python` 없음). 거기서 `--clear`로 다시 만들면 iCloud가 삭제를 맥미니에도 동기화해 **맥미니 venv까지 깨질 수 있다.** 그래서 이 기기용 venv는 iCloud 밖 `~/.venvs/airfare-py311`에 만들고, 스크립트가 있는 쪽을 골라 쓰게 한다.

- [ ] **Step 1: 브랜치와 커밋 작성자 확인**

```bash
cd "$(git rev-parse --show-toplevel)"   # signal-ml-portfolio 루트
git switch main && git pull --ff-only
git switch -c plan-2-demo
git config user.email   # 기대: 55799748+hyde0395@users.noreply.github.com
git config user.name    # 기대: hyde0395
```

값이 다르면 `git config user.email "55799748+hyde0395@users.noreply.github.com"`, `git config user.name hyde0395`로 이 저장소에만 설정한다(`--global` 금지).

- [ ] **Step 2: `scripts/py.sh` 작성**

```sh
#!/bin/sh
# 데이터 추출 스크립트(export_*.py)와 pytest를 돌릴 파이썬을 고른다.
# 항공권 저장소의 .venv는 iCloud로 기기 간에 lib만 넘어와 자주 깨진다(그쪽 CLAUDE.md "머신 이동").
# 거기서 venv를 지우고 다시 만들면 삭제가 다른 기기로 동기화되므로, 기기마다 iCloud 밖
# ~/.venvs/airfare-py311을 두고 그것을 먼저 쓴다. AIRFARE_PYTHON을 주면 그것이 최우선이다.
ROOT="${AIRFARE_ROOT:-$HOME/Documents/airfare-forecasting-ml}"
for PY in "$AIRFARE_PYTHON" "$HOME/.venvs/airfare-py311/bin/python" "$ROOT/.venv/bin/python"; do
  if [ -n "$PY" ] && [ -x "$PY" ]; then exec "$PY" "$@"; fi
done
echo "파이썬을 찾지 못했다: AIRFARE_PYTHON, ~/.venvs/airfare-py311, $ROOT/.venv 중 하나를 준비한다 (CLAUDE.md 참고)" >&2
exit 1
```

```bash
chmod +x scripts/py.sh
```

- [ ] **Step 3: `package.json` scripts 교체**

`"facts"`, `"terrain"`, `"map"` 세 줄을 아래로 바꾸고 `"demo"`, `"pytest"`를 추가한다.

```json
    "facts": "sh scripts/py.sh scripts/export_facts.py",
    "terrain": "sh scripts/py.sh scripts/export_terrain.py",
    "map": "sh scripts/py.sh scripts/export_map.py",
    "demo": "sh scripts/py.sh scripts/export_demo.py",
    "pytest": "sh scripts/py.sh -m pytest scripts/tests -q"
```

- [ ] **Step 4: 이 기기용 venv 만들기 (iCloud 밖)**

```bash
ls ~/.venvs/airfare-py311/bin/python 2>/dev/null || {
  brew list python@3.11 >/dev/null 2>&1 || brew install python@3.11
  mkdir -p ~/.venvs
  "$(brew --prefix python@3.11)/bin/python3.11" -m venv ~/.venvs/airfare-py311
  ~/.venvs/airfare-py311/bin/pip install -r ~/Documents/airfare-forecasting-ml/requirements.txt pytest
}
```

Python 3.12 이상은 쓰지 않는다(neuralprophet 0.9.0이 설치를 거부한다).

- [ ] **Step 5: iCloud 워밍 (항공권 저장소 CLAUDE.md 절차)**

```bash
cd ~/Documents/airfare-forecasting-ml && find src data -type f \
  \( -name "*.py" -o -name "*.csv" -o -name "*.json" -o -name "*.pkl" \) -print0 \
  | xargs -0 -n 40 -P 16 cat > /dev/null
```

- [ ] **Step 6: 기존 파이썬 테스트와 모델 로딩 확인**

```bash
cd "$(git rev-parse --show-toplevel)"
npm run pytest
```

Expected: 모두 통과(기존 export_facts·export_terrain·export_map 테스트).

```bash
sh scripts/py.sh - <<'PY'
import contextlib, io, os, sys
sys.path.insert(0, os.path.expanduser("~/Documents/airfare-forecasting-ml"))
from src.models.v2_predictor import load_predictor
with contextlib.redirect_stdout(io.StringIO()):
    p = load_predictor()
print(type(p).__name__, p.MAX_DTD, sorted(p.q_models))
PY
```

Expected: `V2Predictor 90 ['hi', 'lo', 'margin']`

- [ ] **Step 7: CLAUDE.md 진행 상태 갱신**

`CLAUDE.md`에서 다음 두 줄을 찾아 바꾼다.

찾기: `설계 완료. 계획 1 완료·배포. 계획 3 완료(main 병합 대기). 다음은 계획 2(데모).`
바꾸기: `설계 완료. 계획 1·3 완료·배포(계획 3은 2026-09-24 main 병합, CI 통과). 계획 2(데모) 진행 중: docs/superpowers/plans/2026-09-24-plan-2-demo.md.`

찾기: `| 계획 3: 3D 지형 | ✅ 구현·최종 검토·수정 완료 2026-09-24 · branch \`plan-3-terrain\`(push됨) · **PR로 CI 확인 후 main 병합 대기** |`
바꾸기: `| 계획 3: 3D 지형 | ✅ 완료 2026-09-24 · main 병합(fast-forward) · CI 통과 |`

찾기: `| 계획 2(데모) 계획서 작성 | ⏳ **다음** — \`superpowers:writing-plans\` → 사용자 검토 → 실행 |`
바꾸기: `| 계획 2: 데모 | ⏳ **진행 중** — 계획서 \`docs/superpowers/plans/2026-09-24-plan-2-demo.md\`, branch \`plan-2-demo\` |`

"다른 기기(맥북)에서 이어서 작업하기" 목록의 `- 데이터 스크립트(...)` 줄 끝에 다음 문장을 덧붙인다: ` 2026-09-24부터는 iCloud 밖 \`~/.venvs/airfare-py311\`(Python 3.11)을 기기마다 만들어 쓴다. \`scripts/py.sh\`가 AIRFARE_PYTHON → ~/.venvs/airfare-py311 → 항공권 저장소 .venv 순서로 고른다. 항공권 저장소 .venv를 지우고 다시 만들지 않는다(삭제가 다른 기기로 동기화된다)`

- [ ] **Step 8: 커밋**

```bash
git add scripts/py.sh package.json CLAUDE.md docs/superpowers/plans/2026-09-24-plan-2-demo.md
git commit -m "chore: plan 2 doc; pick python outside iCloud for data scripts; record plan 3 merge"
```

---

### Task 2: `export_demo.py` 순수 함수 (TDD)

**Files:**
- Create: `scripts/export_demo.py`
- Test: `scripts/tests/test_export_demo.py`

**배경 — `recommend_action()`(항공권 저장소 `src/models/v2_predictor.py:181`)의 분기 순서:** `d_now ≤ 1` → BUY_NOW(임박) / `past_optimal`(전체 최저 시점이 이미 지남) → BUY_NOW / `best_day == d_now` → BUY_NOW(지금이 최저) / `saving_pct < 8`(TAU) → BUY_NOW(오차 범위) / 3일 추세 `trend > 0` → DROP_EXPECTED / 그 밖 → WAIT. `confidence`는 `saving_pct ≥ 16` → "높음", `≥ 8` → "보통", 그 밖 `None`이다. 따라서 대기·하락 예상일 때는 항상 "높음"이나 "보통"이다(스펙의 "낮음"은 나오지 않는다). 반환 dict의 `d_now`, `past_optimal`, `best_day`, `saving_pct`로 이유 코드를 다시 판정한다. 판정한 action이 모델의 `action`과 다르면 모델 규칙이 바뀐 것이므로 멈춘다.

**이유 코드 형식** (`savingPct`는 "지금보다 몇 % 싼가"라 양수):

```json
{"action":"DROP_EXPECTED","why":"FALLING","bestDay":30,"bestPrice":171000,"waitDays":12,"saving":16400,"savingPct":8.7,"globalBestDay":30,"confidence":"medium"}
```

| why | action | 문장에 쓰는 값 |
|---|---|---|
| `IMMINENT` | BUY_NOW | — |
| `PAST_OPTIMAL` | BUY_NOW | globalBestDay |
| `AT_LOW` | BUY_NOW | — |
| `SMALL_SAVING` | BUY_NOW | bestDay, savingPct |
| `FALLING` | DROP_EXPECTED | waitDays, bestPrice, savingPct |
| `LATER_LOW` | WAIT | bestDay, waitDays, saving, savingPct |

- [ ] **Step 1: 실패하는 테스트 작성** — `scripts/tests/test_export_demo.py`

```python
# export_demo의 순수 함수 검사: 출발일 범위, 대표 편 고르기, 노선·등급 기준가, 추천 이유 코드,
# 공휴일 코드, 3D 예측 구간 띠, 데모 기본 조합. 모델(pkl)은 쓰지 않는다.
import sys
from pathlib import Path

import pandas as pd
import pytest

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
import export_demo as ed  # noqa: E402


def obs(n, airline="JL", hour=8, stops=0, o="ICN", d="NRT", cls="LCC", ts="2026-09-20 10:00:00", dur=150, price=100_000):
    row = {"fetch_timestamp": ts, "origin": o, "destination": d, "airline_class": cls, "airline": airline,
           "departure_hour": hour, "stops": stops, "duration_minutes": dur, "price": price}
    return [dict(row) for _ in range(n)]


def test_depart_dates_cover_d3_to_d90():
    dates = ed.depart_dates("2026-09-22")
    assert dates[0] == "2026-09-25"
    assert dates[-1] == "2026-12-21"
    assert len(dates) == 88


def test_representative_is_most_observed_recent_flight_with_min_rows():
    kept = pd.DataFrame(
        obs(60, airline="A", hour=8)
        + obs(70, airline="B", hour=9, dur=140)
        + obs(200, airline="C", ts="2026-08-01 10:00:00")   # 최근 21일 밖 → 세지 않는다
        + obs(49, airline="D", cls="FSC")                   # 50행 미만 → 대표 편 없음
    )
    reps = ed.pick_representatives(kept, "2026-09-22")
    assert reps[("ICN_NRT", "LCC")] == {"airline": "B", "departure_hour": 9, "stops": 0, "duration_minutes": 140.0}
    assert reps[("ICN_NRT", "FSC")] is None
    assert reps[("HND_ICN", "LCC")] is None
    assert len(reps) == 12


def test_route_class_base_is_mean_price():
    kept = pd.DataFrame(obs(1, price=100_000) + obs(1, price=300_000) + obs(1, o="KIX", d="ICN", cls="FSC", price=50_000))
    assert ed.route_class_base(kept) == {("ICN_NRT", "LCC"): 200_000.0, ("KIX_ICN", "FSC"): 50_000.0}


def res(**kw):
    base = dict(action="BUY_NOW", d_now=40, past_optimal=False, best_day=30, saving_pct=3.2, best_price=171_000,
                wait_days=10, saving=5_000, global_best_day=30, confidence=None)
    base.update(kw)
    return base


@pytest.mark.parametrize("kw, why", [
    (dict(d_now=1, best_day=1), "IMMINENT"),
    (dict(past_optimal=True, global_best_day=55), "PAST_OPTIMAL"),
    (dict(best_day=40), "AT_LOW"),
    (dict(saving_pct=3.2), "SMALL_SAVING"),
    (dict(action="DROP_EXPECTED", saving_pct=9.0, confidence="보통"), "FALLING"),
    (dict(action="WAIT", saving_pct=17.0, confidence="높음"), "LATER_LOW"),
])
def test_encode_reco_why(kw, why):
    assert ed.encode_reco(res(**kw))["why"] == why


def test_encode_reco_values_and_confidence():
    r = ed.encode_reco(res(action="DROP_EXPECTED", saving_pct=8.66, confidence="보통"))
    assert r == {"action": "DROP_EXPECTED", "why": "FALLING", "bestDay": 30, "bestPrice": 171_000, "waitDays": 10,
                 "saving": 5_000, "savingPct": 8.7, "globalBestDay": 30, "confidence": "medium"}
    # 지금 구매는 확신도를 보이지 않는다(스펙 §6.1)
    assert ed.encode_reco(res(confidence="보통"))["confidence"] is None


def test_encode_reco_rejects_mismatched_action():
    # saving 3%인데 모델이 WAIT라고 하면 모델 쪽 규칙이 바뀐 것 → 조용히 틀린 이유를 쓰지 않고 멈춘다
    with pytest.raises(ValueError):
        ed.encode_reco(res(action="WAIT", saving_pct=3.0))


def test_slug():
    assert ed.slug("Korean Thanksgiving Day") == "korean_thanksgiving_day"
    assert ed.slug("Respect-for-the-Aged Day") == "respect_for_the_aged_day"
    assert ed.slug("New year's Day") == "new_year_s_day"


def test_holiday_codes_nearest_within_three_days():
    hol = [(pd.Timestamp("2026-10-09"), "kr_hangul_day"), (pd.Timestamp("2026-10-12"), "jp_sports_day")]
    got = ed.holiday_codes(["2026-10-05", "2026-10-06", "2026-10-10", "2026-10-11", "2026-10-16"], hol)
    assert got == {"2026-10-06": "kr_hangul_day", "2026-10-10": "kr_hangul_day", "2026-10-11": "jp_sports_day"}


def test_holiday_codes_tie_prefers_first_listed():
    hol = [(pd.Timestamp("2026-10-09"), "kr_a"), (pd.Timestamp("2026-10-09"), "jp_b")]
    assert ed.holiday_codes(["2026-10-08"], hol) == {"2026-10-08": "kr_a"}


def test_band_averages_pct_of_route_class_base_and_skips_missing():
    series = {
        ("ICN_NRT", "LCC"): {"price": [110, None, None], "lo": [90, None, None], "hi": [150, None, None]},
        ("ICN_KIX", "LCC"): {"price": [100, 200, None], "lo": [80, 150, None], "hi": [120, 260, None]},
        ("ICN_HND", "FSC"): None,
    }
    base = {("ICN_NRT", "LCC"): 100.0, ("ICN_KIX", "LCC"): 100.0, ("ICN_HND", "FSC"): 300.0}
    band = ed.build_band(series, base, ["2026-09-25", "2026-09-26", "2026-09-27"], "2026-09-22")
    # 25일: lo (-10, -20) → -15% → ×10 = -150, hi (50, 20) → 35% → 350. 26일: KIX만. 27일: 값 없음 → 뺀다
    assert band == {"asOf": "2026-09-22", "dates": ["2026-09-25", "2026-09-26"], "lo": [-150, 500], "hi": [350, 1600]}


def reco(action, pct):
    return {"action": action, "savingPct": pct}


def test_choose_default_prefers_biggest_drop():
    series = {
        "ICN_NRT/LCC": {"reco": [reco("BUY_NOW", 1.0), reco("DROP_EXPECTED", 9.1)]},
        "ICN_KIX/FSC": {"reco": [reco("DROP_EXPECTED", 12.5), None]},
        "HND_ICN/LCC": None,
    }
    assert ed.choose_default(series, ["2026-09-25", "2026-09-26"]) == {"route": "ICN_KIX", "cabin": "FSC", "date": "2026-09-25"}


def test_choose_default_falls_back_to_wait_then_buy_now():
    wait = {"ICN_NRT/LCC": {"reco": [reco("BUY_NOW", 5.0), reco("WAIT", 16.0)]}}
    assert ed.choose_default(wait, ["2026-09-25", "2026-09-26"])["date"] == "2026-09-26"
    buy = {"ICN_NRT/LCC": {"reco": [reco("BUY_NOW", 1.0), reco("BUY_NOW", 4.0)]}}
    assert ed.choose_default(buy, ["2026-09-25", "2026-09-26"])["date"] == "2026-09-26"
```

- [ ] **Step 2: 실패 확인**

Run: `sh scripts/py.sh -m pytest scripts/tests/test_export_demo.py -q`
Expected: FAIL `ModuleNotFoundError: No module named 'export_demo'`

- [ ] **Step 3: 순수 함수 구현** — `scripts/export_demo.py`

```python
"""public/data/demo.<기준일>.json과 band.<기준일>.json을 만들고 data/facts.json의 demoDefault를 고른다.

- demo: 6개 노선 × LCC/FSC × 출발일 D+3~90마다, 기준일에 샀다면의 예측가·80% 구간·추천을 담는다.
  추천 이유는 문장이 아니라 코드와 값이다. 사이트가 3개 언어 문구 틀로 문장을 조립한다(스펙 §6.2).
- 대표 편: 모델은 "어느 편인지"(항공사·출발 시각·경유)를 받아야 예측하므로, 노선·등급마다 최근 21일
  동안 50행 이상 관측된 편 중 가장 많이 관측된 편을 쓴다(항공권 저장소 추천 시뮬레이션과 같은 기준).
- band: 3-5 챕터의 3D 지형에 얹는 예측 구간 띠. 지형과 같은 "노선·등급 평균 대비 %"로 바꿔
  출발일마다 12개 조합을 평균한다. 원가는 내보내지 않는다(데이터 공개 원칙 §11.4).

실행: npm run demo   (모델을 1,056번 돌려 몇 분 걸린다)
"""
from __future__ import annotations

import contextlib
import gzip
import io
import json
import re
import sys
from collections import Counter
from pathlib import Path

import pandas as pd

sys.path.insert(0, str(Path(__file__).resolve().parent))
from export_facts import AIRFARE_ROOT, FACTS_PATH, METRICS_PATH  # noqa: E402  (AIRFARE_ROOT를 sys.path에 넣는 부수효과 포함)
from export_terrain import ROUTE_CLASS, split_rows  # noqa: E402

ROOT = Path(__file__).resolve().parents[1]
ROUTES = ["ICN_NRT", "NRT_ICN", "ICN_KIX", "KIX_ICN", "ICN_HND", "HND_ICN"]
CABINS = ["LCC", "FSC"]
FIRST_DAY, LAST_DAY = 3, 90          # 모델 V2Predictor.MAX_DTD = 90
REP_WINDOW_DAYS = 21                 # 대표 편: 최근 3주
REP_MIN_ROWS = 50                    # 대표 편: 그동안 50행 이상
FLIGHT = ["airline", "departure_hour", "stops"]
TAU = 8.0                            # recommend_action의 "오차 범위" 기준(%)과 같다
HOLIDAY_WINDOW = 3                   # 공휴일 ±3일(항공권 저장소 is_*_near_holiday와 같다)
MAX_GZIP_BYTES = 500 * 1024          # 스펙 §6.2
BAND_MAX_GZIP_BYTES = 50 * 1024
CONFIDENCE = {"높음": "high", "보통": "medium"}
BUY_WHY = {"IMMINENT", "PAST_OPTIMAL", "AT_LOW", "SMALL_SAVING"}
PREFERENCE = ["DROP_EXPECTED", "WAIT", "BUY_NOW"]


def depart_dates(as_of: str) -> list[str]:
    start = pd.Timestamp(as_of)
    return [(start + pd.Timedelta(days=k)).strftime("%Y-%m-%d") for k in range(FIRST_DAY, LAST_DAY + 1)]


def pick_representatives(kept: pd.DataFrame, as_of: str) -> dict[tuple[str, str], dict | None]:
    """(노선, 등급) → 모델에 넘길 대표 편 속성. 조건을 채우는 편이 없으면 None."""
    end = pd.Timestamp(as_of)
    start = end - pd.Timedelta(days=REP_WINDOW_DAYS - 1)
    day = pd.to_datetime(kept["fetch_timestamp"]).dt.normalize()
    recent = kept[(day >= start) & (day <= end)]
    out: dict[tuple[str, str], dict | None] = {}
    for route in ROUTES:
        origin, dest = route.split("_")
        for cabin in CABINS:
            g = recent[(recent["origin"] == origin) & (recent["destination"] == dest) & (recent["airline_class"] == cabin)]
            counts = g.groupby(FLIGHT).size()
            counts = counts[counts >= REP_MIN_ROWS]
            if counts.empty:
                out[(route, cabin)] = None
                continue
            # 동률이면 키 순서(항공사·시각·경유)로 첫 번째 → 다시 돌려도 같은 편을 고른다
            airline, hour, stops = counts.sort_index().idxmax()
            rows = g[(g["airline"] == airline) & (g["departure_hour"] == hour) & (g["stops"] == stops)]
            out[(route, cabin)] = {"airline": airline, "departure_hour": int(hour), "stops": int(stops),
                                   "duration_minutes": float(rows["duration_minutes"].median())}
    return out


def route_class_base(kept: pd.DataFrame) -> dict[tuple[str, str], float]:
    """노선·등급 평균가. export_terrain.add_route_class_pct와 같은 기준이라 띠가 지형 높이와 맞는다."""
    mean = kept.groupby(ROUTE_CLASS)["price"].mean()
    return {(f"{o}_{d}", c): float(v) for (o, d, c), v in mean.items()}


def encode_reco(res: dict) -> dict:
    """recommend_action() 반환값 → 이유 코드와 값. 분기 순서는 recommend_action과 같다."""
    if res["d_now"] <= 1:
        why = "IMMINENT"
    elif res["past_optimal"]:
        why = "PAST_OPTIMAL"
    elif res["best_day"] == res["d_now"]:
        why = "AT_LOW"
    elif res["saving_pct"] < TAU:
        why = "SMALL_SAVING"
    elif res["action"] == "DROP_EXPECTED":
        why = "FALLING"
    else:
        why = "LATER_LOW"
    expected = "BUY_NOW" if why in BUY_WHY else ("DROP_EXPECTED" if why == "FALLING" else "WAIT")
    if expected != res["action"]:
        raise ValueError(f"이유 {why}는 {expected}인데 모델은 {res['action']} — recommend_action 규칙이 바뀌었는지 확인한다")
    action = res["action"]
    return {
        "action": action,
        "why": why,
        "bestDay": int(res["best_day"]),
        "bestPrice": int(res["best_price"]),
        "waitDays": int(res["wait_days"]),
        "saving": int(res["saving"]),
        "savingPct": round(float(res["saving_pct"]), 1),
        "globalBestDay": int(res["global_best_day"]),
        "confidence": None if action == "BUY_NOW" else CONFIDENCE.get(res["confidence"]),
    }


def slug(name: str) -> str:
    """공휴일 영어 이름 → 코드 조각(소문자, 영숫자 밖은 밑줄)."""
    return re.sub(r"[^a-z0-9]+", "_", name.lower()).strip("_")


def holiday_codes(dates: list[str], holidays: list[tuple[pd.Timestamp, str]]) -> dict[str, str]:
    """출발일 → 가장 가까운(±3일 안) 공휴일 코드. 거리가 같으면 목록 앞쪽(한국 먼저)."""
    out: dict[str, str] = {}
    for d in dates:
        t = pd.Timestamp(d)
        near = [(abs((t - h).days), i, code) for i, (h, code) in enumerate(holidays) if abs((t - h).days) <= HOLIDAY_WINDOW]
        if near:
            out[d] = min(near)[2]
    return out


def build_band(series: dict, base: dict[tuple[str, str], float], dates: list[str], as_of: str) -> dict:
    """출발일마다 12개 조합의 lo·hi를 노선·등급 평균 대비 %로 바꿔 평균한다(%×10 정수, 지형과 같은 양자화)."""
    out = {"asOf": as_of, "dates": [], "lo": [], "hi": []}
    for i, d in enumerate(dates):
        lo, hi = [], []
        for key, s in series.items():
            if s is None or s["price"][i] is None:
                continue
            lo.append((s["lo"][i] / base[key] - 1) * 100)
            hi.append((s["hi"][i] / base[key] - 1) * 100)
        if not lo:
            continue
        out["dates"].append(d)
        out["lo"].append(round(sum(lo) / len(lo) * 10))
        out["hi"].append(round(sum(hi) / len(hi) * 10))
    return out


def choose_default(series: dict, dates: list[str]) -> dict:
    """데모를 처음 열 때 보여 줄 조합. 추천의 대부분이 '지금 구매'라 아무 조합이나 보여 주면 심심하므로
    가격 하락 예상 중 절약률이 가장 큰 것을 고른다(스펙 §6.1). 없으면 대기 추천, 그다음 지금 구매."""
    for action in PREFERENCE:
        best = None
        for key, s in series.items():
            if not s:
                continue
            for i, r in enumerate(s["reco"]):
                if r and r["action"] == action and (best is None or r["savingPct"] > best[0]):
                    best = (r["savingPct"], key, dates[i])
        if best:
            route, cabin = best[1].split("/")
            return {"route": route, "cabin": cabin, "date": best[2]}
    raise SystemExit("예측이 하나도 없다 — 대표 편 조건(REP_WINDOW_DAYS, REP_MIN_ROWS)을 확인한다")
```

(`contextlib`, `gzip`, `io`, `Counter`는 Task 3의 `main()`이 쓴다. 이 Task에서는 import만 해 둔다.)

- [ ] **Step 4: 통과 확인**

Run: `sh scripts/py.sh -m pytest scripts/tests/test_export_demo.py -q`
Expected: `17 passed`

- [ ] **Step 5: 커밋**

```bash
git add scripts/export_demo.py scripts/tests/test_export_demo.py
git commit -m "feat: export_demo pure helpers (representative flight, reason codes, holidays, band)"
```

---

### Task 3: `export_demo.py` 실행부 — 모델을 돌려 JSON 만들기

**Files:**
- Modify: `scripts/export_demo.py` (끝에 추가)
- Create: `public/data/demo.2026-09-22.json`, `public/data/band.2026-09-22.json`
- Modify: `data/facts.json` (`demoDefault`)

- [ ] **Step 1: 실행부 추가** — `scripts/export_demo.py` 끝에 붙인다

```python
def load_holidays(dates: list[str]) -> list[tuple[pd.Timestamp, str]]:
    """workalendar(항공권 저장소 공휴일 피처와 같은 라이브러리)에서 한·일 공휴일과 코드를 모은다.
    한국을 먼저 넣어, 거리가 같으면 한국 공휴일 이름을 보여 준다. 일본 오봉(8/13~16)은 항공권
    저장소가 따로 더하지만 데모 출발일 범위(9월 말~12월)에 없어 넣지 않는다."""
    from workalendar.asia import Japan, SouthKorea
    years = sorted({int(d[:4]) for d in dates} | {int(dates[-1][:4]) + 1})
    out: list[tuple[pd.Timestamp, str]] = []
    for prefix, cal in (("kr", SouthKorea()), ("jp", Japan())):
        for y in years:
            out += [(pd.Timestamp(day), f"{prefix}_{slug(name)}") for day, name in cal.holidays(y)]
    return out


def quiet() -> contextlib.ExitStack:
    """NeuralProphet·V2Predictor가 예측마다 찍는 로그를 삼킨다(항공권 저장소 CLAUDE.md의 시뮬레이션 재현 방법)."""
    buf = io.StringIO()
    stack = contextlib.ExitStack()
    stack.enter_context(contextlib.redirect_stdout(buf))
    stack.enter_context(contextlib.redirect_stderr(buf))
    return stack


def forecast_series(predictor, route: str, cabin: str, rep: dict, dates: list[str], as_of: str) -> dict:
    origin, dest = route.split("_")
    price, lo, hi, reco = [], [], [], []
    for day in dates:
        dep = pd.Timestamp(day)
        with quiet():
            res = predictor.recommend_action(
                origin=origin, destination=dest, departure_date=day, today=as_of,
                airline=rep["airline"], airline_class=cabin, stops=rep["stops"],
                duration_minutes=rep["duration_minutes"], departure_hour=rep["departure_hour"],
                # 수집기(collect_data.py)와 같은 정의: 금·토·일 출발이 주말 편
                is_weekend_flight=int(dep.weekday() >= 4), departure_month=dep.month,
            )
        if res["price_now_low"] is None or res["price_now_high"] is None:
            raise SystemExit("pkl에 예측 구간 모델(q_models)이 없다 — 항공권 저장소에서 재학습이 필요하다")
        price.append(int(res["price_now"]))
        lo.append(int(res["price_now_low"]))
        hi.append(int(res["price_now_high"]))
        reco.append(encode_reco(res))
    return {"price": price, "lo": lo, "hi": hi, "reco": reco}


def write_json(path: Path, obj: dict, limit: int) -> None:
    body = json.dumps(obj, ensure_ascii=False, separators=(",", ":")).encode("utf-8")
    size = len(gzip.compress(body))
    if size > limit:
        raise SystemExit(f"{path.name} gzip {size:,}B > 목표 {limit:,}B — 출발일을 주 단위로 줄여야 한다(스펙 §6.2)")
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_bytes(body)
    print(f"{path.name}: gzip {size:,}B", flush=True)


def main() -> None:
    as_of = json.loads(METRICS_PATH.read_text(encoding="utf-8"))["_meta"]["asOf"]
    raw = pd.read_csv(AIRFARE_ROOT / "data" / "raw" / "flight_prices.csv")
    kept, _ = split_rows(raw, as_of)
    dates = depart_dates(as_of)
    reps = pick_representatives(kept, as_of)

    from src.models.v2_predictor import load_predictor  # NeuralProphet·torch를 끌고 오는 무거운 import라 여기서 한다
    with quiet():
        predictor = load_predictor()

    series: dict[tuple[str, str], dict | None] = {}
    for (route, cabin), rep in reps.items():
        label = "대표 편 없음" if rep is None else f"{rep['airline']} {rep['departure_hour']}시 경유{rep['stops']}"
        print(f"{route}/{cabin}: {label}", flush=True)
        series[(route, cabin)] = None if rep is None else forecast_series(predictor, route, cabin, rep, dates, as_of)

    demo = {
        "asOf": as_of,
        "precomputed": True,
        "routes": ROUTES,
        "cabins": CABINS,
        "dates": dates,
        "holidays": holiday_codes(dates, load_holidays(dates)),
        "series": {f"{r}/{c}": s for (r, c), s in series.items()},
    }
    write_json(ROOT / "public" / "data" / f"demo.{as_of}.json", demo, MAX_GZIP_BYTES)
    write_json(ROOT / "public" / "data" / f"band.{as_of}.json",
               build_band(series, route_class_base(kept), dates, as_of), BAND_MAX_GZIP_BYTES)

    default = choose_default(demo["series"], dates)
    facts = json.loads(FACTS_PATH.read_text(encoding="utf-8"))
    facts["demoDefault"] = default
    FACTS_PATH.write_text(json.dumps(facts, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")

    counts = Counter(r["action"] for s in series.values() if s for r in s["reco"] if r)
    print(f"추천 분포: {dict(counts)}")
    print(f"기본 조합(facts.demoDefault): {default}")
    print("공휴일 코드:", ", ".join(sorted(set(demo["holidays"].values()))))


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 순수 함수 테스트가 여전히 통과하는지 확인**

Run: `sh scripts/py.sh -m pytest scripts/tests/test_export_demo.py -q`
Expected: `17 passed`

- [ ] **Step 3: 실행**

```bash
( caffeinate -i npm run demo 2>&1 | tee /tmp/export_demo.log )
```

Expected (값은 실제 데이터에 따라 다르다):
- 12줄의 `노선/등급: 항공사 N시 경유0`(또는 `대표 편 없음`)
- `demo.2026-09-22.json: gzip …B`(500KB 미만), `band.2026-09-22.json: gzip …B`
- `추천 분포: {'BUY_NOW': …, 'DROP_EXPECTED': …, 'WAIT': …}` — 항공권 저장소 시뮬레이션(92.1 / 5.9 / 2.0%)과 크게 다르지 않아야 한다. DROP_EXPECTED가 0이면 기본 조합이 WAIT나 BUY_NOW로 잡힌다. 그러면 멈추고 사용자에게 알린다(스펙 §6.1은 DROP_EXPECTED 조합을 기대한다)
- `기본 조합(facts.demoDefault): {'route': …, 'cabin': …, 'date': …}`
- `공휴일 코드: …` — **Task 7에서 이 목록을 그대로 쓴다. 출력을 복사해 둔다**

스크립트가 `import` 단계에서 몇 분 넘게 멈춰 있으면 iCloud evict다. Task 1 Step 5 워밍을 다시 하고 재실행한다.

- [ ] **Step 4: 결과 점검**

```bash
node -e "
const d = require('./public/data/demo.2026-09-22.json');
const keys = Object.keys(d.series);
console.log('dates', d.dates.length, d.dates[0], d.dates.at(-1));
console.log('series', keys.length, 'null:', keys.filter(k => !d.series[k]));
const s = d.series[keys.find(k => d.series[k])];
console.log('sample', s.price[0], s.lo[0], s.hi[0], JSON.stringify(s.reco[0]));
const f = require('./data/facts.json'); console.log('demoDefault', f.demoDefault);
const b = require('./public/data/band.2026-09-22.json'); console.log('band', b.dates.length, b.lo[0], b.hi[0]);
"
git diff --stat data/facts.json
```

Expected: `dates 88 2026-09-25 2026-12-21`, `series 12`, 모든 `lo ≤ price ≤ hi`, `facts.json`에는 `demoDefault` 블록만 추가(다른 줄은 그대로).

- [ ] **Step 5: 커밋**

```bash
git add scripts/export_demo.py public/data/demo.2026-09-22.json public/data/band.2026-09-22.json data/facts.json
git commit -m "feat: export precomputed demo forecasts and interval band (as of 2026-09-22)"
```

---

### Task 4: 데모 타입·데이터 접근 계층 (`ForecastSource`)

**Files:**
- Create: `src/demo/types.ts`, `src/demo/source.ts`
- Modify: `src/lib/facts.ts` (demoDefault 스키마)
- Test: `tests/unit/demo-source.test.ts`, `tests/unit/facts.test.ts`

- [ ] **Step 1: `src/demo/types.ts` 작성** (테스트가 import할 타입·상수라 먼저 둔다)

```ts
// 데모 데이터의 타입·상수와 화면이 쓰는 ForecastSource 인터페이스(스펙 §6.3).
// zod 없이 타입과 상수만 있어 초기 JS에 들어가도 가볍다. 검사 코드는 source.ts에 있고, 데모 섹션이 화면
// 가까이 올 때만 불러온다.
export const ROUTES = ['ICN_NRT', 'NRT_ICN', 'ICN_KIX', 'KIX_ICN', 'ICN_HND', 'HND_ICN'] as const;
export type Route = (typeof ROUTES)[number];
export const CABINS = ['LCC', 'FSC'] as const;
export type Cabin = (typeof CABINS)[number];
export const ACTIONS = ['BUY_NOW', 'DROP_EXPECTED', 'WAIT'] as const;
export type Action = (typeof ACTIONS)[number];
// 추천 이유 코드. V2Predictor.recommend_action()의 분기와 1:1이다(scripts/export_demo.py encode_reco)
export const WHYS = ['IMMINENT', 'PAST_OPTIMAL', 'AT_LOW', 'SMALL_SAVING', 'FALLING', 'LATER_LOW'] as const;
export type Why = (typeof WHYS)[number];

export type Reco = {
  action: Action;
  why: Why;
  bestDay: number;
  bestPrice: number;
  waitDays: number;
  saving: number;
  savingPct: number; // 지금보다 몇 % 싼가(양수)
  globalBestDay: number;
  confidence: 'high' | 'medium' | null;
};
export type StripDay = { date: string; price: number | null; holiday: string | null };
export type Forecast = { date: string; price: number; lo: number; hi: number; reco: Reco };
export type Meta = { asOf: string; precomputed: boolean };

export interface ForecastSource {
  meta(): Promise<Meta>;
  getStrip(route: Route, cabin: Cabin): Promise<StripDay[]>;
  getForecast(route: Route, cabin: Cabin, departDate: string): Promise<Forecast | null>;
}

export function demoUrl(dataVersion: string): string {
  return `/data/demo.${dataVersion}.json`;
}
```

- [ ] **Step 2: 실패하는 테스트 작성** — `tests/unit/demo-source.test.ts`

```ts
// 데모 데이터 접근 계층 검사: 막대·결과 조회, 대표 편 없는 조합, 불러오기 실패와 다시 시도,
// 형식 검사, 그리고 커밋된 demo.json 자체(CI에서 항공권 저장소 없이 돈다).
import { readFileSync } from 'node:fs';
import { gzipSync } from 'node:zlib';
import { describe, expect, it, vi } from 'vitest';
import { demoSchema, StaticForecastSource } from '@/demo/source';
import { demoUrl } from '@/demo/types';
import { facts } from '@/lib/facts';

const reco = { action: 'DROP_EXPECTED', why: 'FALLING', bestDay: 30, bestPrice: 171000, waitDays: 12, saving: 16400, savingPct: 8.7, globalBestDay: 30, confidence: 'medium' };
const fixture = {
  asOf: '2026-09-22', precomputed: true, routes: ['ICN_NRT'], cabins: ['LCC', 'FSC'],
  dates: ['2026-09-25', '2026-09-26'],
  holidays: { '2026-09-26': 'kr_chuseok' },
  series: {
    'ICN_NRT/LCC': { price: [187400, null], lo: [152000, null], hi: [231000, null], reco: [reco, null] },
    'ICN_NRT/FSC': null,
  },
};
const ok = (body: unknown) => Promise.resolve(new Response(JSON.stringify(body), { status: 200 }));

describe('StaticForecastSource', () => {
  it('meta: 기준일과 미리 계산 여부', async () => {
    const src = new StaticForecastSource('/d.json', vi.fn(() => ok(fixture)) as unknown as typeof fetch);
    expect(await src.meta()).toEqual({ asOf: '2026-09-22', precomputed: true });
  });

  it('getStrip: 날짜마다 예측가(없으면 null)와 공휴일 코드', async () => {
    const src = new StaticForecastSource('/d.json', vi.fn(() => ok(fixture)) as unknown as typeof fetch);
    expect(await src.getStrip('ICN_NRT', 'LCC')).toEqual([
      { date: '2026-09-25', price: 187400, holiday: null },
      { date: '2026-09-26', price: null, holiday: 'kr_chuseok' },
    ]);
  });

  it('대표 편이 없는 조합은 모든 날이 null', async () => {
    const src = new StaticForecastSource('/d.json', vi.fn(() => ok(fixture)) as unknown as typeof fetch);
    expect((await src.getStrip('ICN_NRT', 'FSC')).map((d) => d.price)).toEqual([null, null]);
    expect(await src.getForecast('ICN_NRT', 'FSC', '2026-09-25')).toBeNull();
  });

  it('getForecast: 값이 있는 날은 결과, 없는 날·모르는 날은 null', async () => {
    const src = new StaticForecastSource('/d.json', vi.fn(() => ok(fixture)) as unknown as typeof fetch);
    expect(await src.getForecast('ICN_NRT', 'LCC', '2026-09-25')).toEqual({ date: '2026-09-25', price: 187400, lo: 152000, hi: 231000, reco });
    expect(await src.getForecast('ICN_NRT', 'LCC', '2026-09-26')).toBeNull();
    expect(await src.getForecast('ICN_NRT', 'LCC', '2027-01-01')).toBeNull();
  });

  it('파일은 한 번만 받는다', async () => {
    const fetcher = vi.fn(() => ok(fixture));
    const src = new StaticForecastSource('/d.json', fetcher as unknown as typeof fetch);
    await src.meta();
    await src.getStrip('ICN_NRT', 'LCC');
    expect(fetcher).toHaveBeenCalledTimes(1);
    expect(fetcher).toHaveBeenCalledWith('/d.json');
  });

  it('실패하면 reject하고, 다음 호출은 새로 받는다(다시 시도 버튼)', async () => {
    const fetcher = vi.fn()
      .mockReturnValueOnce(Promise.resolve(new Response('no', { status: 404 })))
      .mockReturnValueOnce(ok(fixture));
    const src = new StaticForecastSource('/d.json', fetcher as unknown as typeof fetch);
    await expect(src.meta()).rejects.toThrow('HTTP 404');
    await expect(src.meta()).resolves.toEqual({ asOf: '2026-09-22', precomputed: true });
    expect(fetcher).toHaveBeenCalledTimes(2);
  });

  it('배열 길이가 dates와 다르면 형식 오류', () => {
    const bad = structuredClone(fixture) as typeof fixture;
    bad.series['ICN_NRT/LCC']!.price = [1];
    expect(() => demoSchema.parse(bad)).toThrow(/demo-series-length-mismatch/);
  });
});

describe('커밋된 demo.json', () => {
  const body = readFileSync(`public${demoUrl(facts.dataVersion)}`);
  const data = demoSchema.parse(JSON.parse(body.toString('utf-8')));

  it('스키마를 통과하고 gzip 500KB 이하', () => {
    expect(data.asOf).toBe(facts.dataVersion);
    expect(data.dates).toHaveLength(88);
    expect(gzipSync(body).length).toBeLessThanOrEqual(500 * 1024);
  });

  it('facts.demoDefault 조합은 예측이 있고 가격 하락 예상이다', async () => {
    const src = new StaticForecastSource('/d.json', vi.fn(() => ok(data)) as unknown as typeof fetch);
    const { route, cabin, date } = facts.demoDefault;
    const f = await src.getForecast(route, cabin, date);
    expect(f?.reco.action).toBe('DROP_EXPECTED');
  });

  it('모든 예측은 lo ≤ 예측가 ≤ hi', () => {
    for (const s of Object.values(data.series)) {
      if (!s) continue;
      s.price.forEach((p, i) => {
        if (p === null) return;
        expect(s.lo[i]!).toBeLessThanOrEqual(p);
        expect(p).toBeLessThanOrEqual(s.hi[i]!);
      });
    }
  });
});
```

(Task 3 출력의 추천 분포에 DROP_EXPECTED가 있었다는 전제다. 없었다면 Task 3 Step 3에서 이미 멈췄어야 한다.)

- [ ] **Step 3: 실패 확인**

Run: `npx vitest run tests/unit/demo-source.test.ts`
Expected: FAIL `Failed to resolve import "@/demo/source"`

- [ ] **Step 4: `src/demo/source.ts` 작성**

```ts
// 미리 계산한 demo.<기준일>.json을 읽는 ForecastSource(스펙 §6.3). 모델 서버가 생기면 같은 인터페이스의
// ApiForecastSource를 만들고 DemoApp에서 생성하는 한 줄만 바꾼다. zod가 들어 있어 DemoApp이 dynamic
// import로만 부른다(초기 JS에 넣지 않기 위해).
import { z } from 'zod';
import {
  ACTIONS, CABINS, ROUTES, WHYS,
  type Cabin, type Forecast, type ForecastSource, type Meta, type Route, type StripDay,
} from './types';

const isoDate = z.string().regex(/^\d{4}-\d{2}-\d{2}$/);
const won = z.number().int().nonnegative().nullable();
const reco = z.object({
  action: z.enum(ACTIONS),
  why: z.enum(WHYS),
  bestDay: z.number().int(),
  bestPrice: z.number().int(),
  waitDays: z.number().int(),
  saving: z.number().int(),
  savingPct: z.number(),
  globalBestDay: z.number().int(),
  confidence: z.enum(['high', 'medium']).nullable(),
});
const series = z.object({ price: z.array(won), lo: z.array(won), hi: z.array(won), reco: z.array(reco.nullable()) });

export const demoSchema = z
  .object({
    asOf: isoDate,
    precomputed: z.boolean(),
    routes: z.array(z.enum(ROUTES)),
    cabins: z.array(z.enum(CABINS)),
    dates: z.array(isoDate).min(1),
    holidays: z.record(isoDate, z.string().regex(/^(kr|jp)_[a-z0-9_]+$/)),
    series: z.record(z.string(), series.nullable()),
  })
  .superRefine((d, ctx) => {
    for (const [key, s] of Object.entries(d.series)) {
      if (s && [s.price, s.lo, s.hi, s.reco].some((a) => a.length !== d.dates.length)) {
        // 영어 머리말은 e2e가 "이 모듈이 초기 청크에 없는지" 찾을 때 쓰는 표식이다(tests/e2e/demo.spec.ts)
        ctx.addIssue({ code: 'custom', message: `demo-series-length-mismatch: ${key} 배열 길이가 dates와 다르다` });
      }
    }
  });
export type DemoData = z.infer<typeof demoSchema>;

export class StaticForecastSource implements ForecastSource {
  private readonly url: string;
  private readonly fetcher: typeof fetch;
  private data: Promise<DemoData> | null = null;

  // 기본값을 fetch 그대로 넘기면 this.fetcher(...)로 부를 때 브라우저가 "Illegal invocation"을 던진다 → 감싼다
  constructor(url: string, fetcher: typeof fetch = (input, init) => fetch(input, init)) {
    this.url = url;
    this.fetcher = fetcher;
  }

  private load(): Promise<DemoData> {
    // 실패한 Promise를 남겨 두면 "다시 시도"가 계속 같은 실패를 돌려주므로, 실패하면 비워 다음 호출이 새로 받게 한다
    this.data ??= this.fetcher(this.url)
      .then(async (res) => {
        if (!res.ok) throw new Error(`${this.url}: HTTP ${res.status}`);
        return demoSchema.parse(await res.json());
      })
      .catch((e: unknown) => {
        this.data = null;
        throw e;
      });
    return this.data;
  }

  async meta(): Promise<Meta> {
    const d = await this.load();
    return { asOf: d.asOf, precomputed: d.precomputed };
  }

  async getStrip(route: Route, cabin: Cabin): Promise<StripDay[]> {
    const d = await this.load();
    const s = d.series[`${route}/${cabin}`] ?? null;
    return d.dates.map((date, i) => ({ date, price: s?.price[i] ?? null, holiday: d.holidays[date] ?? null }));
  }

  async getForecast(route: Route, cabin: Cabin, departDate: string): Promise<Forecast | null> {
    const d = await this.load();
    const s = d.series[`${route}/${cabin}`];
    const i = d.dates.indexOf(departDate);
    if (!s || i < 0) return null;
    const price = s.price[i], lo = s.lo[i], hi = s.hi[i], reco = s.reco[i];
    // 하나라도 비면 그 날은 "예측 없음"이다(막대에서도 고를 수 없게 그린다)
    if (price === null || lo === null || hi === null || reco === null) return null;
    return { date: departDate, price, lo, hi, reco };
  }
}
```

- [ ] **Step 5: `src/lib/facts.ts`에 demoDefault 스키마 추가**

import 줄 아래에 추가:

```ts
import { CABINS, ROUTES } from '@/demo/types';
```

`factsSchema`의 `dataVersion: isoDate,` 다음 줄에 추가:

```ts
  // 데모를 처음 열 때 고를 조합(스펙 §6.1). export_demo.py가 가격 하락 예상 중 절약률이 가장 큰 조합으로 채운다
  demoDefault: z.object({ route: z.enum(ROUTES), cabin: z.enum(CABINS), date: isoDate }),
```

`tests/unit/facts.test.ts`의 `'필수 수치가 들어 있다'` 테스트 끝에 추가:

```ts
    expect(facts.demoDefault.date).toMatch(/^\d{4}-\d{2}-\d{2}$/);
```

- [ ] **Step 6: 통과 확인**

Run: `npx vitest run tests/unit/demo-source.test.ts tests/unit/facts.test.ts && npm run typecheck`
Expected: 모두 PASS, 타입 오류 없음

- [ ] **Step 7: 커밋**

```bash
git add src/demo/types.ts src/demo/source.ts src/lib/facts.ts tests/unit/demo-source.test.ts tests/unit/facts.test.ts
git commit -m "feat: ForecastSource interface and StaticForecastSource over demo.json"
```

---

### Task 5: 출발일 막대 순수 계산 (`strip.ts`)

**Files:**
- Create: `src/demo/strip.ts`
- Test: `tests/unit/demo-strip.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/unit/demo-strip.test.ts`

```ts
// 출발일 막대 계산 검사: 예측 없는 날 건너뛰기, 처음·끝, 누른 위치 → 막대 번호, 높이 비율.
import { describe, expect, it } from 'vitest';
import { barScale, edgeSelectable, indexFromX, nearestSelectable, stepSelectable } from '@/demo/strip';

const d = (prices: (number | null)[]) => prices.map((price) => ({ price }));

describe('stepSelectable', () => {
  it('예측 없는 날을 건너뛴다', () => {
    expect(stepSelectable(d([1, null, 3]), 0, 1)).toBe(2);
    expect(stepSelectable(d([1, null, 3]), 2, -1)).toBe(0);
  });
  it('끝에서는 제자리', () => {
    expect(stepSelectable(d([1, null, 3]), 2, 1)).toBe(2);
    expect(stepSelectable(d([1, 2]), 0, -1)).toBe(0);
  });
});

describe('edgeSelectable', () => {
  it('처음·끝 고를 수 있는 날, 없으면 -1', () => {
    expect(edgeSelectable(d([null, 2, 3, null]), 'first')).toBe(1);
    expect(edgeSelectable(d([null, 2, 3, null]), 'last')).toBe(2);
    expect(edgeSelectable(d([null, null]), 'first')).toBe(-1);
    expect(edgeSelectable(d([null, null]), 'last')).toBe(-1);
  });
});

describe('nearestSelectable', () => {
  it('가까운 쪽, 같은 거리면 앞쪽', () => {
    expect(nearestSelectable(d([1, null, null, 4]), 2)).toBe(3);
    expect(nearestSelectable(d([1, null, null, 4]), 1)).toBe(0);
    expect(nearestSelectable(d([1, null, 3]), 1)).toBe(0);
    expect(nearestSelectable(d([1, 2]), 1)).toBe(1);
  });
  it('하나도 없으면 -1', () => expect(nearestSelectable(d([null]), 0)).toBe(-1));
});

describe('indexFromX', () => {
  it('막대 폭으로 나누고 범위를 벗어나면 양 끝', () => {
    expect(indexFromX(0, 100, 10)).toBe(0);
    expect(indexFromX(99.9, 100, 10)).toBe(9);
    expect(indexFromX(150, 100, 10)).toBe(9);
    expect(indexFromX(-5, 100, 10)).toBe(0);
    expect(indexFromX(10, 0, 10)).toBe(0);
  });
});

describe('barScale', () => {
  it('최저가는 바닥 0.15, 최고가는 1', () => {
    const s = barScale([100, null, 200]);
    expect(s(100)).toBeCloseTo(0.15);
    expect(s(200)).toBe(1);
    expect(s(150)).toBeCloseTo(0.575);
  });
  it('값이 모두 같으면 가운데 높이', () => expect(barScale([5, 5])(5)).toBe(0.6));
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/demo-strip.test.ts`
Expected: FAIL `Failed to resolve import "@/demo/strip"`

- [ ] **Step 3: `src/demo/strip.ts` 작성**

```ts
// 출발일 막대의 순수 계산: 고를 수 있는 날(예측이 있는 날)만 오가기, 누른 위치 → 막대 번호, 막대 높이.
// 예측이 없는 날은 키보드로 이동할 때도 건너뛴다(스펙 §6.4).
type Day = { price: number | null };
const selectable = (d: Day | undefined) => d !== undefined && d.price !== null;

// dir 방향의 다음 고를 수 있는 날. 없으면 제자리(끝에서 더 가지 않는다)
export function stepSelectable(days: Day[], from: number, dir: 1 | -1): number {
  for (let i = from + dir; i >= 0 && i < days.length; i += dir) if (selectable(days[i])) return i;
  return from;
}

export function edgeSelectable(days: Day[], edge: 'first' | 'last'): number {
  if (edge === 'first') return days.findIndex(selectable);
  for (let i = days.length - 1; i >= 0; i--) if (selectable(days[i])) return i;
  return -1;
}

// 예측 없는 날을 누르거나 노선을 바꿔 고른 날이 비었을 때, 가장 가까운 고를 수 있는 날(같은 거리면 앞쪽)
export function nearestSelectable(days: Day[], i: number): number {
  for (let r = 0; r < days.length; r++) {
    if (selectable(days[i - r])) return i - r;
    if (selectable(days[i + r])) return i + r;
  }
  return -1;
}

export function indexFromX(x: number, width: number, n: number): number {
  if (n <= 0 || width <= 0) return 0;
  return Math.min(n - 1, Math.max(0, Math.floor((x / width) * n)));
}

// 막대 높이(0..1). 가장 싼 날도 막대가 보이도록 바닥을 0.15로 둔다
const FLOOR = 0.15;
export function barScale(prices: (number | null)[]): (p: number) => number {
  const vals = prices.filter((p): p is number => p !== null);
  if (vals.length === 0) return () => FLOOR;
  const min = Math.min(...vals), max = Math.max(...vals);
  if (max === min) return () => 0.6;
  return (p) => FLOOR + ((1 - FLOOR) * (p - min)) / (max - min);
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run tests/unit/demo-strip.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/demo/strip.ts tests/unit/demo-strip.test.ts
git commit -m "feat: date strip helpers (skip empty days, pointer index, bar scale)"
```

---

### Task 6: i18n — `fixed1`·`md` 형식과 `prefill`

**Files:**
- Modify: `src/lib/i18n.ts`
- Test: `tests/unit/i18n.test.ts`

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/unit/i18n.test.ts` 끝에 붙인다(이미 `describe`, `expect`, `it`를 vitest에서 import하고 있다. 아래 import는 파일 위쪽 i18n import 줄에 `prefill`을 더한다)

파일 위쪽 `@/lib/i18n` import에 `prefill`, `formatValue`, `interpolate`가 모두 들어가게 한다(이미 있는 이름은 두 번 쓰지 않는다).

```ts
describe('데모용 형식', () => {
  it('fixed1: 소수 첫째 자리, 부호 없음', () => {
    expect(formatValue(8.66, 'fixed1', 'ko')).toBe('8.7');
    expect(formatValue(12, 'fixed1', 'en')).toBe('12.0');
  });
  it('md: 월·일·요일(연도 없음)', () => {
    const ko = formatValue('2026-11-14', 'md', 'ko');
    expect(ko).toContain('11월');
    expect(ko).toContain('14일');
    expect(ko).toContain('토');
    expect(formatValue('2026-11-14', 'md', 'en')).toMatch(/Sat.*Nov.*14/);
    expect(formatValue('2026-11-14', 'md', 'ja')).toMatch(/11月14日.*土/);
  });
});

describe('prefill', () => {
  it('facts 자리표시만 채우고 {v.…}는 남긴다', () => {
    const source = { model: { interval: { level: 80 } } };
    expect(prefill('{model.interval.level}% · {v.price}원 · {v.date|md}', source, 'ko')).toBe('80% · {v.price}원 · {v.date|md}');
  });
  it('남긴 {v.…}는 나중에 interpolate로 채운다', () => {
    const t = prefill('{v.price}원', {}, 'ko');
    expect(interpolate(t, { v: { price: 187400 } }, 'ko')).toBe('187,400원');
  });
  it('facts에 없는 경로는 여전히 throw', () => {
    expect(() => prefill('{model.nope}', {}, 'ko')).toThrow('Missing fact');
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/i18n.test.ts`
Expected: FAIL (`prefill` 없음, `Cannot format … as "fixed1"`)

- [ ] **Step 3: `src/lib/i18n.ts` 수정**

`formatValue`의 `if (format === 'plain' …` 줄 바로 다음에 추가:

```ts
  if (format === 'fixed1' && typeof value === 'number') {
    return new Intl.NumberFormat(intl, { minimumFractionDigits: 1, maximumFractionDigits: 1 }).format(value);
  }
  // 데모의 출발일: 연도 없이 월·일·요일(예: 11월 14일 (토)). 같은 해 안의 날짜만 다루기 때문이다
  if (format === 'md' && typeof value === 'string') {
    return new Intl.DateTimeFormat(intl, { month: 'long', day: 'numeric', weekday: 'short', timeZone: 'UTC' })
      .format(new Date(`${value}T00:00:00Z`));
  }
```

`interpolate` 함수 전체를 아래로 바꾼다(채우는 부분을 `fill`로 빼서 `prefill`과 함께 쓴다. `replace` 콜백 안에서 같은 전역 정규식으로 `replace`를 또 부르면 `lastIndex`가 꼬이므로 `fill`은 정규식을 쓰지 않는다).

```ts
function fill(source: unknown, path: string, format: string | undefined, locale: Locale): string {
  const value = lookup(source, path);
  // 자리표시 경로가 facts에 없으면 조용히 빈 문자열로 두지 않고 throw한다. 그래야 화면에
  // "{...}"가 그대로 찍히는 대신 빌드(테스트)에서 바로 잡힌다.
  if (value === undefined) throw new Error(`Missing fact for placeholder {${path}}`);
  return formatValue(value, format, locale);
}

export function interpolate(template: string, source: unknown, locale: Locale): string {
  return template.replace(PLACEHOLDER, (_, path: string, format: string | undefined) => fill(source, path, format, locale));
}

// 서버에서 facts 자리표시만 먼저 채우고, {v.…}(데모 화면이 실행 중에 채우는 값)는 그대로 남긴다.
// 그래서 클라이언트에는 facts 전체가 아니라 채워진 문구만 넘어간다.
export function prefill(template: string, source: unknown, locale: Locale): string {
  return template.replace(PLACEHOLDER, (whole: string, path: string, format: string | undefined) =>
    path.startsWith('v.') ? whole : fill(source, path, format, locale));
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx vitest run tests/unit/i18n.test.ts tests/unit/content.test.ts`
Expected: PASS. `md` 결과가 Node의 ICU에 따라 조금 다르면(예: 괄호·공백), 테스트는 부분 문자열로만 보므로 통과해야 한다. 실패하면 실제 출력을 확인하고 테스트의 정규식을 그 출력에 맞춘다. `Intl` 옵션은 바꾸지 않는다.

- [ ] **Step 5: 커밋**

```bash
git add src/lib/i18n.ts tests/unit/i18n.test.ts
git commit -m "feat: fixed1/md formats and prefill for client-side demo text"
```

---

### Task 7: 데모 문구 3개 언어 + 이유 문장 조립

**Files:**
- Modify: `content/ko.json`, `content/en.json`, `content/ja.json` (`demo` 블록)
- Create: `src/demo/reason.ts`
- Modify: `src/lib/content.ts` (`DemoTexts`, `demoTexts`)
- Modify: `tests/unit/content.test.ts`
- Test: `tests/unit/demo-reason.test.ts`

**문구 톤:** 슬로건·광고 문장을 쓰지 않는다. 사실만 담백하게 쓴다(사용자 피드백). 한국어는 사용자가 검토하고, 영어는 사용자, 일본어는 사용자가 섭외한 검수자가 확인한다. 이 Task의 영·일 문구는 초안이다.

- [ ] **Step 1: `src/demo/reason.ts` 작성**

```ts
// 추천 이유 코드 → 문장. demo.json은 한국어 문장이 아니라 코드와 값만 담는다(스펙 §6.2).
// 문장은 언어별 문구 틀(content/*.json의 demo.why.*)에 값을 채워 만든다. 틀 안의 {v.…}가 여기서 채우는 값이다.
import type { DemoTexts } from '@/lib/content';
import { interpolate, type Locale } from '@/lib/i18n';
import type { Reco } from './types';

export function reasonText(texts: DemoTexts, reco: Reco, locale: Locale): string {
  return interpolate(texts.why[reco.why], { v: reco }, locale);
}

// 확신도는 대기·하락 예상일 때만 보인다(스펙 §6.1). 모델은 '높음'·'보통' 두 단계만 낸다
export function confidenceText(texts: DemoTexts, reco: Reco): string | null {
  if (reco.action === 'BUY_NOW' || reco.confidence === null) return null;
  return `${texts.confidence.label}: ${texts.confidence[reco.confidence]}`;
}

// 문구 검사(content.test.ts)가 {v.…} 자리표시를 채워 볼 때 쓰는 견본 값.
// 데모 문구에 새 {v.이름}을 넣으면 여기에도 추가해야 테스트가 통과한다.
export const SAMPLE_VALUES = {
  date: '2026-11-14',
  asOf: '2026-09-22',
  price: 187400,
  lo: 152000,
  hi: 231000,
  cabin: 'LCC',
  holiday: 'Hangul Day',
  action: 'DROP EXPECTED',
  bestDay: 30,
  bestPrice: 171000,
  waitDays: 12,
  saving: 16400,
  savingPct: 8.7,
  globalBestDay: 35,
};
```

- [ ] **Step 2: `src/lib/content.ts`에 `DemoTexts`·`demoTexts` 추가**

import 줄을 바꾼다: `import { createT, type Locale } from './i18n';` → `import { createT, prefill, type Locale } from './i18n';`

파일 끝에 추가:

```ts
export type DemoTexts = Dict['demo'];

// 데모 문구: facts 수치는 서버에서 채우고 {v.…}는 남긴 채, 그 언어 것만 클라이언트 DemoApp에 넘긴다.
// (세 언어 사전과 facts 전체를 클라이언트 JS에 싣지 않기 위해)
export function demoTexts(locale: Locale): DemoTexts {
  const walk = (node: unknown): unknown =>
    typeof node === 'string'
      ? prefill(node, facts, locale)
      : Object.fromEntries(Object.entries(node as Record<string, unknown>).map(([k, v]) => [k, walk(v)]));
  return walk(dictionaries[locale].demo) as DemoTexts;
}
```

- [ ] **Step 3: 공휴일 코드 확인**

```bash
node -e "const d=require('./public/data/demo.2026-09-22.json'); console.log([...new Set(Object.values(d.holidays))].sort().join('\n'))"
```

출력된 코드 **하나도 빠짐없이** 아래 Step 4의 `demo.holidays`에 넣는다. 아래 JSON의 `holidays`는 workalendar 이름으로 예상한 코드다. 실제 코드와 다르면 **키를 실제 코드로 바꾸고**, 아래 표에서 뜻이 같은 이름표를 쓴다. 출력에 없는 키는 지운다(세 언어 모두 같은 키).

| 뜻 | ko | en | ja |
|---|---|---|---|
| 추석 연휴(전날·당일·다음날 모두) | 추석 연휴 | Chuseok holidays | 秋夕の連休 |
| 개천절 | 개천절 | National Foundation Day (Korea) | 開天節(韓国) |
| 한글날 | 한글날 | Hangul Day (Korea) | ハングルの日(韓国) |
| 성탄절 | 성탄절 | Christmas | クリスマス |
| 일본 국민의 휴일 | 국민의 휴일(일본) | Citizens' Holiday (Japan) | 国民の休日 |
| 추분의 날 | 추분의 날(일본) | Autumnal Equinox Day (Japan) | 秋分の日 |
| 경로의 날 | 경로의 날(일본) | Respect for the Aged Day (Japan) | 敬老の日 |
| 스포츠의 날 | 스포츠의 날(일본) | Sports Day (Japan) | スポーツの日 |
| 문화의 날 | 문화의 날(일본) | Culture Day (Japan) | 文化の日 |
| 근로감사의 날 | 근로감사의 날(일본) | Labor Thanksgiving Day (Japan) | 勤労感謝の日 |

표에 없는 코드가 나오면 멈추고 사용자에게 이름표를 묻는다.

- [ ] **Step 4: 세 언어 `demo` 블록 추가**

각 파일에서 `"stack": {` 블록 **앞**에 넣는다(키 순서는 세 파일이 같게). `holidays`는 Step 3 결과대로 고친다.

`content/ko.json`:

```json
  "demo": {
    "heading": "지금 살까, 기다릴까",
    "intro": "노선과 등급, 출발일을 고르면 기준일에 샀을 때의 예측가와 {model.interval.level}% 예측 구간, 추천을 보여 줍니다.",
    "basis": "노선·등급마다 최근 관측이 가장 많은 편 하나를 골라 예측했습니다. 모델 서버 없이 미리 계산한 값입니다.",
    "sentence": "[route] 가는 [cabin]를 [date]에 타려면, 지금 살까요?",
    "routeLabel": "노선",
    "cabinToggle": "등급 {v.cabin}, 눌러서 바꾸기",
    "routes": {
      "ICN_NRT": "인천 → 나리타",
      "NRT_ICN": "나리타 → 인천",
      "ICN_KIX": "인천 → 간사이",
      "KIX_ICN": "간사이 → 인천",
      "ICN_HND": "인천 → 하네다",
      "HND_ICN": "하네다 → 인천"
    },
    "strip": {
      "label": "출발일",
      "valuetext": "{v.date|md}, 예측가 {v.price}원",
      "holiday": "공휴일 주변: {v.holiday}",
      "legendHoliday": "공휴일 주변 출발일",
      "legendNoData": "예측 없음"
    },
    "money": "{v.price}원",
    "result": {
      "price": "예측가",
      "range": "{model.interval.level}% 예측 구간",
      "rangeAria": "{model.interval.level}% 예측 구간 {v.lo}원에서 {v.hi}원"
    },
    "why": {
      "IMMINENT": "출발이 임박했습니다. 지금 사는 편이 낫습니다.",
      "PAST_OPTIMAL": "예상 최저 시점(D-{v.globalBestDay})은 이미 지났습니다. 남은 기간에는 지금이 가장 쌀 것으로 예측됩니다.",
      "AT_LOW": "지금이 예상 최저가입니다. 기다리면 오를 가능성이 높습니다.",
      "SMALL_SAVING": "D-{v.bestDay}의 예상 최저가와 차이가 {v.savingPct|fixed1}%로, 예측 오차 범위 안입니다.",
      "FALLING": "가격이 내려가는 중입니다. {v.waitDays}일 더 지켜볼 만합니다. 예상 저점은 약 {v.bestPrice}원(지금보다 {v.savingPct|fixed1}% 낮음)입니다.",
      "LATER_LOW": "D-{v.bestDay}(약 {v.waitDays}일 뒤)에 사면 약 {v.saving}원({v.savingPct|fixed1}%) 쌀 것으로 예측됩니다."
    },
    "confidence": { "label": "확신도", "high": "높음", "medium": "보통" },
    "asOf": "미리 계산된 예측 · {v.asOf|date} 기준",
    "announce": "{v.date|md} 출발, 예측가 {v.price}원, {v.action}.",
    "loading": "데모 데이터를 불러오는 중입니다.",
    "error": "데모 데이터를 불러오지 못했습니다.",
    "retry": "다시 시도",
    "noForecast": "이 노선·등급은 대표 편이 없어 예측이 없습니다.",
    "noJs": "데모는 JavaScript를 켜면 동작합니다.",
    "holidays": {
      "kr_korean_thanksgiving_day": "추석 연휴",
      "kr_day_after_korean_thanksgiving_day": "추석 연휴",
      "kr_national_foundation_day": "개천절",
      "kr_hangul_day": "한글날",
      "jp_autumnal_equinox_day": "추분의 날(일본)",
      "jp_sports_day": "스포츠의 날(일본)",
      "jp_culture_day": "문화의 날(일본)",
      "jp_labour_thanksgiving_day": "근로감사의 날(일본)"
    }
  },
```

`content/en.json`:

```json
  "demo": {
    "heading": "Buy now or wait?",
    "intro": "Pick a route, cabin, and departure date to see the predicted price if bought on the reference date, its {model.interval.level}% prediction interval, and a recommendation.",
    "basis": "For each route and cabin, the prediction uses the one flight observed most often recently. The values were computed in advance, without a model server.",
    "sentence": "Flying [route] on [cabin], departing [date] — buy now?",
    "routeLabel": "Route",
    "cabinToggle": "Cabin {v.cabin}, press to switch",
    "routes": {
      "ICN_NRT": "Incheon → Narita",
      "NRT_ICN": "Narita → Incheon",
      "ICN_KIX": "Incheon → Kansai",
      "KIX_ICN": "Kansai → Incheon",
      "ICN_HND": "Incheon → Haneda",
      "HND_ICN": "Haneda → Incheon"
    },
    "strip": {
      "label": "Departure date",
      "valuetext": "{v.date|md}, predicted ₩{v.price}",
      "holiday": "near a holiday: {v.holiday}",
      "legendHoliday": "Departures near a holiday",
      "legendNoData": "No prediction"
    },
    "money": "₩{v.price}",
    "result": {
      "price": "Predicted price",
      "range": "{model.interval.level}% prediction interval",
      "rangeAria": "{model.interval.level}% prediction interval from ₩{v.lo} to ₩{v.hi}"
    },
    "why": {
      "IMMINENT": "Departure is very close. Buying now is the better choice.",
      "PAST_OPTIMAL": "The predicted lowest point (D-{v.globalBestDay}) has already passed. For the remaining days, now is predicted to be the cheapest.",
      "AT_LOW": "Now is the predicted lowest price. Waiting is more likely to cost more.",
      "SMALL_SAVING": "The gap to the predicted low at D-{v.bestDay} is {v.savingPct|fixed1}%, within the model's error range.",
      "FALLING": "The price is falling. It may be worth watching for {v.waitDays} more days: the predicted low is about ₩{v.bestPrice} ({v.savingPct|fixed1}% below now).",
      "LATER_LOW": "Buying at D-{v.bestDay} (about {v.waitDays} days from now) is predicted to save about ₩{v.saving} ({v.savingPct|fixed1}%)."
    },
    "confidence": { "label": "Confidence", "high": "High", "medium": "Medium" },
    "asOf": "Precomputed prediction · as of {v.asOf|date}",
    "announce": "Departing {v.date|md}, predicted ₩{v.price}, {v.action}.",
    "loading": "Loading demo data.",
    "error": "Couldn't load the demo data.",
    "retry": "Try again",
    "noForecast": "There is no representative flight for this route and cabin, so there is no prediction.",
    "noJs": "The demo needs JavaScript to run.",
    "holidays": {
      "kr_korean_thanksgiving_day": "Chuseok holidays",
      "kr_day_after_korean_thanksgiving_day": "Chuseok holidays",
      "kr_national_foundation_day": "National Foundation Day (Korea)",
      "kr_hangul_day": "Hangul Day (Korea)",
      "jp_autumnal_equinox_day": "Autumnal Equinox Day (Japan)",
      "jp_sports_day": "Sports Day (Japan)",
      "jp_culture_day": "Culture Day (Japan)",
      "jp_labour_thanksgiving_day": "Labor Thanksgiving Day (Japan)"
    }
  },
```

`content/ja.json`:

```json
  "demo": {
    "heading": "今買う? 待つ?",
    "intro": "路線・クラス・出発日を選ぶと、基準日に買った場合の予測価格と{model.interval.level}%予測区間、おすすめを表示します。",
    "basis": "路線・クラスごとに、直近で最も多く観測された便を一つ選んで予測しました。モデルサーバーなしで事前に計算した値です。",
    "sentence": "[route]の[cabin]で[date]に出発するなら、今買うべき?",
    "routeLabel": "路線",
    "cabinToggle": "クラス {v.cabin}、押して切り替え",
    "routes": {
      "ICN_NRT": "仁川 → 成田",
      "NRT_ICN": "成田 → 仁川",
      "ICN_KIX": "仁川 → 関西",
      "KIX_ICN": "関西 → 仁川",
      "ICN_HND": "仁川 → 羽田",
      "HND_ICN": "羽田 → 仁川"
    },
    "strip": {
      "label": "出発日",
      "valuetext": "{v.date|md}、予測価格 {v.price}ウォン",
      "holiday": "祝日付近: {v.holiday}",
      "legendHoliday": "祝日付近の出発日",
      "legendNoData": "予測なし"
    },
    "money": "{v.price}ウォン",
    "result": {
      "price": "予測価格",
      "range": "{model.interval.level}%予測区間",
      "rangeAria": "{model.interval.level}%予測区間 {v.lo}ウォンから{v.hi}ウォン"
    },
    "why": {
      "IMMINENT": "出発が間近です。今買うほうがよいです。",
      "PAST_OPTIMAL": "予測される最安時点(D-{v.globalBestDay})はすでに過ぎました。残りの期間では今が最も安いと予測されます。",
      "AT_LOW": "今が予測最安値です。待つと値上がりする可能性が高いです。",
      "SMALL_SAVING": "D-{v.bestDay}の予測最安値との差は{v.savingPct|fixed1}%で、予測誤差の範囲内です。",
      "FALLING": "価格は下がっています。あと{v.waitDays}日様子を見る価値があります。予測安値は約{v.bestPrice}ウォン(今より{v.savingPct|fixed1}%安い)です。",
      "LATER_LOW": "D-{v.bestDay}(約{v.waitDays}日後)に買うと、約{v.saving}ウォン({v.savingPct|fixed1}%)安くなると予測されます。"
    },
    "confidence": { "label": "確信度", "high": "高", "medium": "中" },
    "asOf": "事前計算した予測 · {v.asOf|date}時点",
    "announce": "{v.date|md}出発、予測価格 {v.price}ウォン、{v.action}。",
    "loading": "デモデータを読み込んでいます。",
    "error": "デモデータを読み込めませんでした。",
    "retry": "再試行",
    "noForecast": "この路線・クラスは代表便がないため予測がありません。",
    "noJs": "デモを使うにはJavaScriptを有効にしてください。",
    "holidays": {
      "kr_korean_thanksgiving_day": "秋夕の連休",
      "kr_day_after_korean_thanksgiving_day": "秋夕の連休",
      "kr_national_foundation_day": "開天節(韓国)",
      "kr_hangul_day": "ハングルの日(韓国)",
      "jp_autumnal_equinox_day": "秋分の日",
      "jp_sports_day": "スポーツの日",
      "jp_culture_day": "文化の日",
      "jp_labour_thanksgiving_day": "勤労感謝の日"
    }
  },
```

- [ ] **Step 5: `tests/unit/content.test.ts` 수정** — `{v.…}`를 견본 값으로 채워 본다

import에 추가:

```ts
import { SAMPLE_VALUES } from '@/demo/reason';
```

`const flat = …` 다음 줄에 추가:

```ts
// 데모 문구의 {v.…}는 실행 중에 채워지므로 견본 값으로 채워 본다(src/demo/reason.ts)
const source = { ...facts, v: SAMPLE_VALUES };
```

`모든 자리표시가 facts에서 해석된다` 테스트 안의 `interpolate(text, facts, locale)`를 `interpolate(text, source, locale)`로 바꾼다.

- [ ] **Step 6: 실패하는 테스트 작성** — `tests/unit/demo-reason.test.ts`

```ts
// 데모 문구 검사: 이유 코드 6종이 세 언어로 조립되는지, 확신도 표시 조건, 질문 문장 틀,
// 서버에서 facts만 채운 문구, 그리고 커밋된 demo.json의 공휴일 코드에 이름표가 모두 있는지.
import { readFileSync } from 'node:fs';
import { describe, expect, it } from 'vitest';
import { confidenceText, reasonText } from '@/demo/reason';
import { WHYS, demoUrl, type Reco } from '@/demo/types';
import { demoTexts } from '@/lib/content';
import { facts } from '@/lib/facts';
import { LOCALES } from '@/lib/i18n';

const reco = (over: Partial<Reco>): Reco => ({
  action: 'BUY_NOW', why: 'AT_LOW', bestDay: 30, bestPrice: 171000, waitDays: 12, saving: 16400,
  savingPct: 8.7, globalBestDay: 35, confidence: null, ...over,
});

describe.each(LOCALES)('%s', (locale) => {
  const texts = demoTexts(locale);

  it('facts 수치는 채워지고 {v.…}는 남는다', () => {
    expect(texts.intro).not.toContain('{model.');
    expect(texts.money).toContain('{v.price}');
  });

  it('질문 문장에 노선·등급·날짜 자리가 하나씩 있다', () => {
    for (const slot of ['[route]', '[cabin]', '[date]']) expect(texts.sentence.split(slot)).toHaveLength(2);
  });

  it.each(WHYS)('이유 %s: 자리표시가 모두 채워진다', (why) => {
    const s = reasonText(texts, reco({ why }), locale);
    expect(s).not.toMatch(/[{}]/);
    expect(s.length).toBeGreaterThan(5);
  });

  it('하락 예상 이유에 값이 들어간다', () => {
    const s = reasonText(texts, reco({ action: 'DROP_EXPECTED', why: 'FALLING', confidence: 'medium' }), locale);
    expect(s).toContain('171,000');
    expect(s).toContain('8.7');
    expect(s).toContain('12');
  });

  it('확신도는 대기·하락일 때만', () => {
    expect(confidenceText(texts, reco({ confidence: 'high' }))).toBeNull();
    expect(confidenceText(texts, reco({ action: 'WAIT', why: 'LATER_LOW', confidence: 'high' }))).toContain(texts.confidence.high);
  });

  it('demo.json의 공휴일 코드마다 이름표가 있다', () => {
    const demo = JSON.parse(readFileSync(`public${demoUrl(facts.dataVersion)}`, 'utf-8')) as { holidays: Record<string, string> };
    const labels = texts.holidays as Record<string, string>;
    for (const code of new Set(Object.values(demo.holidays))) expect(labels[code], `${locale}: ${code}`).toBeTruthy();
  });
});
```

- [ ] **Step 7: 통과 확인**

Run: `npx vitest run tests/unit/demo-reason.test.ts tests/unit/content.test.ts && npm run typecheck`
Expected: PASS. `공휴일 코드마다 이름표가 있다`가 실패하면 실패 메시지의 코드를 Step 3 표에 따라 세 파일에 추가한다.

- [ ] **Step 8: 커밋**

```bash
git add content/ src/demo/reason.ts src/lib/content.ts tests/unit/content.test.ts tests/unit/demo-reason.test.ts
git commit -m "feat: demo copy in three languages and reason-code sentences"
```

---

### Task 8: 데모 화면 — 섹션, 질문 문장, 출발일 막대, 결과

**Files:**
- Create: `src/components/demo/DemoApp.tsx`, `src/components/demo/DateStrip.tsx`, `src/components/demo/DemoResult.tsx`, `src/components/sections/Demo.tsx`
- Modify: `src/components/HomePage.tsx`
- Modify: `src/styles/globals.css`

이 Task의 동작 확인은 Task 11 e2e가 한다. 여기서는 타입 검사, 빌드, 브라우저 눈 확인까지 한다.

- [ ] **Step 1: `src/components/demo/DemoResult.tsx`**

```tsx
// 데모 결과(스펙 §6.1): 예측가 → 80% 예측 구간 띠 → 추천 배지 + 이유 → (대기·하락일 때만) 확신도.
// 예측가는 계획 4에서 플립 글자판으로 바뀔 자리라 data-flip을 붙여 둔다(지금은 완성된 값만 보인다).
import type { DemoTexts } from '@/lib/content';
import { interpolate, type Locale } from '@/lib/i18n';
import { confidenceText, reasonText } from '@/demo/reason';
import type { Action, Forecast } from '@/demo/types';

// 배지는 세 언어 공통 영어 표기(GATE 제목처럼)다. 색만으로 뜻을 전하지 않도록 늘 글자를 함께 쓴다(§9.4)
export const BADGE: Record<Action, string> = { BUY_NOW: 'BUY NOW', DROP_EXPECTED: 'DROP EXPECTED', WAIT: 'WAIT' };

// 띠 막대 안에서 lo~hi가 차지하는 구간(%). 양 끝을 비워 예측가 표시가 가장자리에 붙지 않게 한다
const BAND_FROM = 10;
const BAND_TO = 90;

export function DemoResult({ forecast, locale, texts }: { forecast: Forecast; locale: Locale; texts: DemoTexts }) {
  const { price, lo, hi, reco } = forecast;
  const money = (v: number) => interpolate(texts.money, { v: { price: v } }, locale);
  const mid = hi > lo ? BAND_FROM + ((BAND_TO - BAND_FROM) * (price - lo)) / (hi - lo) : 50;
  const conf = confidenceText(texts, reco);
  return (
    <div className="demo-result">
      <p className="demo-price">
        <span className="eyebrow">{texts.result.price}</span>
        <span className="mono" data-flip>{money(price)}</span>
      </p>
      <div className="band" role="img" aria-label={interpolate(texts.result.rangeAria, { v: { lo, hi } }, locale)}>
        <span className="band-fill" style={{ left: `${BAND_FROM}%`, right: `${100 - BAND_TO}%` }} />
        <span className="band-mid" style={{ left: `${mid}%` }} />
      </div>
      <p className="band-labels mono">
        <span>{money(lo)}</span>
        <span className="muted">{texts.result.range}</span>
        <span>{money(hi)}</span>
      </p>
      <p className={`badge is-${reco.action.toLowerCase()}`}>{BADGE[reco.action]}</p>
      <p>{reasonText(texts, reco, locale)}</p>
      {conf && <p className="muted">{conf}</p>}
    </div>
  );
}
```

- [ ] **Step 2: `src/components/demo/DateStrip.tsx`**

```tsx
// 출발일 막대 = 날짜 선택기(스펙 §6.1, §9.2). 막대 하나가 출발일 하나이고, 높이는 기준일에 샀을 때의 예측가다.
// 공휴일 주변은 호박색 + ◆ 표식 + 이름표로 보여 준다(색만으로 뜻을 전하지 않는다, §9.4). 예측이 없는 날은
// 회색 점선이고 고를 수 없다(§6.4).
import { useRef, type KeyboardEvent, type PointerEvent } from 'react';
import type { DemoTexts } from '@/lib/content';
import { formatValue, interpolate, type Locale } from '@/lib/i18n';
import { barScale, edgeSelectable, indexFromX, nearestSelectable, stepSelectable } from '@/demo/strip';
import type { StripDay } from '@/demo/types';

type Props = { days: StripDay[]; index: number; onChange: (i: number) => void; locale: Locale; texts: DemoTexts };

export function holidayLabel(texts: DemoTexts, code: string): string {
  return (texts.holidays as Record<string, string>)[code] ?? code;
}

export function DateStrip({ days, index, onChange, locale, texts }: Props) {
  const ref = useRef<HTMLDivElement>(null);
  const dragging = useRef(false);
  const scale = barScale(days.map((d) => d.price));
  const first = edgeSelectable(days, 'first');
  const last = edgeSelectable(days, 'last');
  const cur = days[index];

  const pick = (clientX: number) => {
    const el = ref.current;
    if (!el) return;
    const r = el.getBoundingClientRect();
    const i = nearestSelectable(days, indexFromX(clientX - r.left, r.width, days.length));
    if (i >= 0 && i !== index) onChange(i);
  };

  const onPointerDown = (e: PointerEvent<HTMLDivElement>) => {
    dragging.current = true;
    e.currentTarget.setPointerCapture(e.pointerId); // 막대 밖으로 끌고 나가도 계속 따라가게
    pick(e.clientX);
  };

  const onKeyDown = (e: KeyboardEvent<HTMLDivElement>) => {
    const next =
      e.key === 'ArrowRight' || e.key === 'ArrowUp' ? stepSelectable(days, index, 1)
      : e.key === 'ArrowLeft' || e.key === 'ArrowDown' ? stepSelectable(days, index, -1)
      : e.key === 'Home' ? first
      : e.key === 'End' ? last
      : null;
    if (next === null) return;
    e.preventDefault(); // 화살표·Home·End가 페이지를 스크롤하지 않게
    if (next >= 0 && next !== index) onChange(next);
  };

  const valueText =
    cur && cur.price !== null
      ? interpolate(texts.strip.valuetext, { v: { date: cur.date, price: cur.price } }, locale) +
        (cur.holiday ? `, ${interpolate(texts.strip.holiday, { v: { holiday: holidayLabel(texts, cur.holiday) } }, locale)}` : '')
      : undefined;

  return (
    <div className="strip-wrap">
      <div
        ref={ref}
        className="strip"
        role="slider"
        tabIndex={0}
        aria-label={texts.strip.label}
        aria-valuemin={first}
        aria-valuemax={last}
        aria-valuenow={index}
        aria-valuetext={valueText}
        aria-disabled={first < 0 ? true : undefined}
        onKeyDown={onKeyDown}
        onPointerDown={onPointerDown}
        onPointerMove={(e) => { if (dragging.current) pick(e.clientX); }}
        onPointerUp={() => { dragging.current = false; }}
        onPointerCancel={() => { dragging.current = false; }}
      >
        {days.map((d, i) => (
          <span
            key={d.date}
            aria-hidden="true"
            className={['strip-bar', d.price === null && 'is-empty', d.holiday && 'is-holiday', i === index && 'is-selected']
              .filter(Boolean)
              .join(' ')}
            style={d.price === null ? undefined : { height: `${scale(d.price) * 100}%` }}
          />
        ))}
      </div>
      {days.length > 0 && (
        <p className="strip-axis mono" aria-hidden="true">
          <span>{formatValue(days[0].date, 'md', locale)}</span>
          <span>{formatValue(days[days.length - 1].date, 'md', locale)}</span>
        </p>
      )}
      {cur?.holiday && (
        <p className="strip-holiday">
          <span aria-hidden="true">◆ </span>
          {holidayLabel(texts, cur.holiday)}
        </p>
      )}
      <p className="strip-legend">
        <span><span className="legend-mark is-holiday" aria-hidden="true" />{texts.strip.legendHoliday}</span>
        <span><span className="legend-mark is-empty" aria-hidden="true" />{texts.strip.legendNoData}</span>
      </p>
    </div>
  );
}
```

- [ ] **Step 3: `src/components/demo/DemoApp.tsx`**

```tsx
'use client';
// 데모 조작 화면(스펙 §6.1 B1): 질문 문장 속 선택지(노선·등급·날짜) + 출발일 막대 + 결과.
// 데이터는 섹션이 화면 가까이 올 때 처음 불러오고(스펙 §8.1), zod가 든 데이터 모듈은 그때 dynamic import해
// 초기 JS에 싣지 않는다. 화면은 ForecastSource 인터페이스만 알기 때문에, 나중에 API로 바꿔도 이 파일은 그대로다.
import { Fragment, useCallback, useEffect, useRef, useState, type ReactNode } from 'react';
import { DateStrip } from './DateStrip';
import { BADGE, DemoResult } from './DemoResult';
import type { DemoTexts } from '@/lib/content';
import { formatValue, interpolate, type Locale } from '@/lib/i18n';
import { reasonText } from '@/demo/reason';
import { edgeSelectable, nearestSelectable } from '@/demo/strip';
import { ROUTES, type Cabin, type Forecast, type ForecastSource, type Route, type StripDay } from '@/demo/types';

type Props = {
  locale: Locale;
  texts: DemoTexts;
  dataUrl: string;
  initial: { route: Route; cabin: Cabin; date: string };
  initialAsOf: string;
};
type Status = 'idle' | 'loading' | 'error' | 'ready';
type Slot = 'route' | 'cabin' | 'date';

// 막대를 끌거나 화살표를 연달아 누르는 동안은 읽지 않고, 멈춘 뒤 한 번만 알린다(스펙 §9.3)
const ANNOUNCE_DELAY_MS = 700;
// 섹션이 화면 아래 600px 안으로 들어오면 미리 불러온다(스크롤해 도착했을 때 이미 준비되도록)
const LOAD_MARGIN = '600px 0px';

export function DemoApp({ locale, texts, dataUrl, initial, initialAsOf }: Props) {
  const root = useRef<HTMLDivElement>(null);
  const source = useRef<ForecastSource | null>(null);
  const wantedDate = useRef(initial.date); // 노선·등급을 바꿔도 같은 출발일을 유지하려고 기억한다
  const touched = useRef(false);           // 첫 표시 때는 알리지 않고, 방문자가 조작한 뒤에만 알린다
  const [status, setStatus] = useState<Status>('idle');
  const [asOf, setAsOf] = useState(initialAsOf);
  const [route, setRoute] = useState<Route>(initial.route);
  const [cabin, setCabin] = useState<Cabin>(initial.cabin);
  const [days, setDays] = useState<StripDay[]>([]);
  const [index, setIndex] = useState(-1);
  const [forecast, setForecast] = useState<Forecast | null>(null);
  const [announce, setAnnounce] = useState('');

  const load = useCallback(async () => {
    setStatus('loading');
    try {
      if (!source.current) {
        const { StaticForecastSource } = await import('@/demo/source');
        source.current = new StaticForecastSource(dataUrl);
      }
      setAsOf((await source.current.meta()).asOf);
      setStatus('ready');
    } catch (e) {
      console.warn('데모 데이터를 불러오지 못했다', e);
      setStatus('error');
    }
  }, [dataUrl]);

  useEffect(() => {
    const el = root.current;
    if (!el) return;
    if (typeof IntersectionObserver === 'undefined') {
      void load();
      return;
    }
    const io = new IntersectionObserver((entries) => {
      if (entries.some((e) => e.isIntersecting)) {
        io.disconnect();
        void load();
      }
    }, { rootMargin: LOAD_MARGIN });
    io.observe(el);
    return () => io.disconnect();
  }, [load]);

  // 노선·등급이 바뀌면 막대를 새로 받는다. 전에 고른 출발일이 비어 있으면 가장 가까운 날로 옮긴다
  useEffect(() => {
    const src = source.current;
    if (status !== 'ready' || !src) return;
    let alive = true;
    src.getStrip(route, cabin).then(
      (next) => {
        if (!alive) return;
        setDays(next);
        const i = next.findIndex((d) => d.date === wantedDate.current);
        setIndex(i >= 0 ? nearestSelectable(next, i) : edgeSelectable(next, 'first'));
      },
      () => { if (alive) setStatus('error'); },
    );
    return () => { alive = false; };
  }, [status, route, cabin]);

  useEffect(() => {
    const src = source.current;
    const day = days[index];
    if (!src || !day) {
      setForecast(null);
      return;
    }
    wantedDate.current = day.date;
    let alive = true;
    src.getForecast(route, cabin, day.date).then((f) => { if (alive) setForecast(f); });
    return () => { alive = false; };
  }, [days, index, route, cabin]);

  useEffect(() => {
    if (!forecast || !touched.current) return;
    const id = window.setTimeout(() => {
      const head = interpolate(texts.announce, { v: { date: forecast.date, price: forecast.price, action: BADGE[forecast.reco.action] } }, locale);
      setAnnounce(`${head} ${reasonText(texts, forecast.reco, locale)}`);
    }, ANNOUNCE_DELAY_MS);
    return () => window.clearTimeout(id);
  }, [forecast, texts, locale]);

  const day = days[index];
  const slots: Record<Slot, ReactNode> = {
    route: (
      <select
        className="demo-slot"
        aria-label={texts.routeLabel}
        value={route}
        onChange={(e) => { touched.current = true; setRoute(e.target.value as Route); }}
      >
        {ROUTES.map((r) => <option key={r} value={r}>{texts.routes[r]}</option>)}
      </select>
    ),
    cabin: (
      <button
        type="button"
        className="demo-slot demo-cabin"
        aria-label={interpolate(texts.cabinToggle, { v: { cabin } }, locale)}
        onClick={() => { touched.current = true; setCabin((c) => (c === 'LCC' ? 'FSC' : 'LCC')); }}
      >
        {cabin}
      </button>
    ),
    date: <span className="demo-slot is-date">{day ? formatValue(day.date, 'md', locale) : '—'}</span>,
  };

  return (
    <div ref={root} className="demo-app">
      {/* 문장 틀 "[route] 가는 [cabin]를 [date]에 …"을 잘라 자리마다 조작 요소를 끼운다. 어순이 언어마다 달라 틀로 둔다 */}
      <p className="demo-question">
        {texts.sentence.split(/\[(route|cabin|date)\]/).map((part, i) =>
          i % 2 === 1 ? <Fragment key={i}>{slots[part as Slot]}</Fragment> : part)}
      </p>
      {(status === 'loading' || (status === 'ready' && days.length === 0)) && <p className="muted">{texts.loading}</p>}
      {status === 'error' && (
        <div className="demo-error" role="alert">
          <span>{texts.error}</span>
          <button type="button" className="pill" onClick={() => void load()}>{texts.retry}</button>
        </div>
      )}
      {status === 'ready' && days.length > 0 && (
        <>
          <DateStrip days={days} index={index} locale={locale} texts={texts} onChange={(i) => { touched.current = true; setIndex(i); }} />
          {forecast ? <DemoResult forecast={forecast} locale={locale} texts={texts} /> : <p className="muted">{texts.noForecast}</p>}
        </>
      )}
      <p className="demo-asof mono">{interpolate(texts.asOf, { v: { asOf } }, locale)}</p>
      <p className="sr-only" aria-live="polite">{announce}</p>
    </div>
  );
}
```

- [ ] **Step 4: `src/components/sections/Demo.tsx`**

```tsx
// 인터랙티브 데모 섹션(스펙 §6): 제목·설명·기준일은 서버 HTML에 두고, 조작 화면(DemoApp)은 브라우저에서 그린다.
// JS가 꺼져 있으면 noscript 안내가 보인다.
import { DemoApp } from '../demo/DemoApp';
import { demoUrl } from '@/demo/types';
import { demoTexts, getT } from '@/lib/content';
import { facts } from '@/lib/facts';
import type { Locale } from '@/lib/i18n';

export function Demo({ locale }: { locale: Locale }) {
  const t = getT(locale);
  return (
    <section id="demo" data-section="demo" className="wrap" aria-labelledby="demo-h">
      <p className="eyebrow">DEMO</p>
      <h2 id="demo-h" className="display">{t('demo.heading')}</h2>
      <p>{t('demo.intro')}</p>
      <p className="muted">{t('demo.basis')}</p>
      <noscript><p className="demo-nojs">{t('demo.noJs')}</p></noscript>
      <DemoApp
        locale={locale}
        texts={demoTexts(locale)}
        dataUrl={demoUrl(facts.dataVersion)}
        initial={facts.demoDefault}
        initialAsOf={facts.dataVersion}
      />
    </section>
  );
}
```

- [ ] **Step 5: `src/components/HomePage.tsx` — 섹션 끼우기**

import 목록에 추가: `import { Demo } from './sections/Demo';`

`{/* 계획 2: 데모 섹션이 여기에 들어간다 */}` 줄을 `<Demo locale={locale} />`로 바꾼다.

- [ ] **Step 6: `src/styles/globals.css` — 데모 스타일**

`.not-found { … }` 줄 다음에 추가:

```css
.sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); clip-path: inset(50%); white-space: nowrap; }
```

`@media (max-width: 640px) { .vtable …` 줄 다음에 추가:

```css
/* 데모(스펙 §6.1): 질문 문장 속 밑줄 선택지, 출발일 막대, 결과 카드 */
.demo-app { margin-top: 32px; display: grid; gap: 20px; }
.demo-question { font-size: clamp(1.15rem, 2.4vw, 1.5rem); line-height: 1.9; }
.demo-slot { font: inherit; color: var(--tx); background: none; border: 0; border-bottom: 2px solid var(--dot); border-radius: 0; padding: 0 2px; cursor: pointer; }
select.demo-slot { appearance: none; -webkit-appearance: none; }
select.demo-slot option { background: var(--bg2); color: var(--tx); }
.demo-slot.is-date { border-bottom-color: var(--amb); cursor: default; }
.strip { display: flex; align-items: flex-end; gap: 1px; height: 120px; padding-top: 12px; border-bottom: 1px solid var(--line);
  cursor: pointer; touch-action: pan-y; /* 가로로 끌면 날짜를 고르고, 세로로 밀면 페이지가 스크롤된다 */ }
.strip:focus-visible { outline-offset: 6px; }
.strip-bar { position: relative; flex: 1; min-width: 2px; background: var(--dot); opacity: 0.55; border-radius: 1px 1px 0 0; }
.strip-bar.is-holiday { background: var(--amb); }
.strip-bar.is-holiday::after { content: ''; position: absolute; left: 50%; top: -9px; width: 5px; height: 5px; background: var(--amb); transform: translateX(-50%) rotate(45deg); }
.strip-bar.is-selected { opacity: 1; box-shadow: 0 0 0 1px var(--tx); }
.strip-bar.is-empty { height: 100%; background: none; border: 1px dashed rgba(238, 243, 255, 0.35); opacity: 1; }
.strip-axis { display: flex; justify-content: space-between; margin-top: 6px; font-size: 0.75rem; color: var(--mute); }
.strip-holiday { margin-top: 6px; color: var(--amb); font-size: 0.9rem; }
.strip-legend { display: flex; flex-wrap: wrap; gap: 16px; margin-top: 8px; font-size: 0.8rem; color: var(--mute); }
.legend-mark { display: inline-block; width: 10px; height: 10px; margin-right: 6px; vertical-align: -1px; }
.legend-mark.is-holiday { background: var(--amb); transform: rotate(45deg) scale(0.7); }
.legend-mark.is-empty { border: 1px dashed rgba(238, 243, 255, 0.5); }
.demo-result { display: grid; gap: 12px; padding: 20px; border: 1px solid var(--line); border-radius: 12px; background: rgba(19, 32, 58, 0.5); }
.demo-price { display: grid; gap: 4px; }
.demo-price .mono { font-size: clamp(1.75rem, 5vw, 2.5rem); line-height: 1.1; }
.band { position: relative; height: 10px; border-radius: 5px; background: var(--line); }
.band-fill { position: absolute; top: 0; bottom: 0; border-radius: 5px; background: rgba(143, 184, 255, 0.45); }
.band-mid { position: absolute; top: -4px; bottom: -4px; width: 2px; margin-left: -1px; background: var(--tx); }
.band-labels { display: flex; flex-wrap: wrap; justify-content: space-between; gap: 8px; font-size: 0.85rem; }
.badge { justify-self: start; padding: 4px 10px; border: 1px solid currentColor; border-radius: 4px; font: 600 0.8rem var(--font-mono); letter-spacing: 0.14em; }
.badge.is-buy_now { color: var(--tx); }
.badge.is-drop_expected { color: var(--dot); }
.badge.is-wait { color: var(--amb); }
.demo-error { display: flex; flex-wrap: wrap; align-items: center; gap: 12px; }
.demo-asof { font-size: 0.8rem; color: var(--mute); }
```

3D 카드 규칙 세 곳에 `#demo`를 더한다(데모 글도 3D 위에서 반투명 카드를 받게):

- `html[data-3d="on"] :is(.chapter, #about, #stack, #contact) > :is(p, h2, h3, ul, ol, table, a, div, dl):not(.pass) { max-width: 560px; }` → `:is(.chapter, #about, #demo, #stack, #contact)`
- `html[data-3d="on"] :is(.chapter, #about, #stack, #contact) {` (가로 그라데이션 배경) → `:is(.chapter, #about, #demo, #stack, #contact)`
- `@media (max-width: 767px)` 안의 `html[data-3d="on"] :is(.chapter, #about, #stack, #contact) {` → `:is(.chapter, #about, #demo, #stack, #contact)`

- [ ] **Step 7: 타입 검사·단위 테스트·빌드**

Run: `npm run typecheck && npm test && npm run build`
Expected: 모두 성공. `out/index.html`에 `id="demo"`가 있다: `grep -c 'id="demo"' out/index.html out/en/index.html out/ja/index.html` → 각 1

- [ ] **Step 8: 브라우저 눈 확인**

`npm run dev`를 띄우고 `http://localhost:3000/#demo`, `/en/#demo`, `/ja/#demo`를 연다. 확인할 것:
- 처음 선택이 `facts.demoDefault` 조합이고 배지가 `DROP EXPECTED`
- 막대를 누르거나 끌면 날짜가 바뀌고, 문장 속 날짜(호박색 밑줄)와 결과가 따라 바뀜
- 공휴일 막대 위 ◆ 표식, 고른 날이 공휴일이면 이름표
- 휴대폰 폭(개발자 도구 375px)에서 가로 스크롤 없음, 막대가 화면 안에 들어옴

- [ ] **Step 9: 커밋**

```bash
git add src/components/demo src/components/sections/Demo.tsx src/components/HomePage.tsx src/styles/globals.css
git commit -m "feat: demo section with sentence selectors, date strip slider, and result card"
```

---

### Task 9: 3D — 데모 장면과 3-5 예측 구간 띠

**Files:**
- Modify: `src/three/data.ts`, `src/three/shaders.ts`, `src/three/scenes.ts`, `src/three/activeScene.ts`, `src/three/TerrainPoints.tsx`, `src/three/TerrainScene.tsx`
- Test: `tests/unit/three-data.test.ts`, `tests/unit/three-scenes.test.ts`

**좌표:** 띠는 출발일마다 lo~hi를 세로 점 6개로 세운다. x는 "기준일 시점의 예약 일수"(출발일 − 기준일, 3~90)다. 지형 데이터는 기준일까지만 있으므로 이 자리가 지형의 앞 가장자리("오늘")다. z는 출발일인데, 지형의 출발일 축은 날짜 번호(가까운 쪽은 매일, 먼 쪽은 주 1회라 간격이 고르지 않음)라서 이웃한 두 날짜 사이를 날수로 보간한 소수 번호를 쓴다. 높이는 지형과 같은 `heightPerPct`다.

- [ ] **Step 1: 실패하는 테스트 추가** — `tests/unit/three-data.test.ts`

import 줄을 바꾼다:

```ts
import { buildPointCloud, curveWeight, dateIndex, loadSceneData, mapPosition, terrainPosition, type Band, type MapData, type Terrain } from '@/three/data';
```

`const map: MapData = …;` 다음에 추가:

```ts
const band: Band = { asOf: '2026-09-22', dates: ['2026-10-02', '2026-12-01'], lo: [-100, 0], hi: [100, 0] };
```

`describe('loadSceneData', …)` 안의 첫 번째 테스트 `'기준일로 파일을 찾아 두 JSON을 검사해 돌려준다'` 전체를 아래로 바꾼다:

```ts
  it('기준일로 파일을 찾아 세 JSON(지형·지도·띠)을 검사해 돌려준다', async () => {
    const fetcher = vi.fn((url: string) => ok(url.includes('terrain') ? terrain : url.includes('band') ? band : map));
    const data = await loadSceneData('2026-09-22', fetcher as unknown as typeof fetch);
    expect(fetcher).toHaveBeenCalledWith('/data/terrain.2026-09-22.json');
    expect(fetcher).toHaveBeenCalledWith('/data/band.2026-09-22.json');
    expect(data.terrain.dates).toHaveLength(3);
    expect(data.band.dates).toHaveLength(2);
  });
```

파일 끝에 추가:

```ts
describe('예측 구간 띠(band, kind 4)', () => {
  it('dateIndex: 이웃한 두 출발일 사이를 날수로 보간하고, 범위 밖은 null', () => {
    expect(dateIndex(['2026-10-01', '2026-10-03', '2026-10-07'], '2026-10-02')).toBe(0.5);
    expect(dateIndex(['2026-10-01', '2026-10-03', '2026-10-07'], '2026-10-06')).toBe(1.75);
    expect(dateIndex(['2026-10-01', '2026-10-03'], '2026-10-01')).toBe(0);
    expect(dateIndex(['2026-10-01', '2026-10-03'], '2026-12-01')).toBeNull();
  });

  it('출발일마다 lo~hi를 점 6개 세로 줄로, 기준일 시점 예약 일수 자리에 세운다', () => {
    const plain = buildPointCloud(terrain, map, { noiseStride: 1 });
    const pc = buildPointCloud(terrain, map, { noiseStride: 1, band });
    expect(pc.count).toBe(plain.count + 6); // 12-01은 지형 출발일 범위 밖이라 건너뛴다
    const s = plain.count;
    for (let k = 0; k < 6; k++) expect(pc.kind[s + k]).toBe(4);
    // 10-02는 기준일 10일 뒤 → x = ((100-10)/100-0.5)*16 = 6.4, 출발일 번호 1(가운데) → z = 0, lo -10% → y = -0.4
    const at = (arr: Float32Array, i: number) => [arr[i * 3], arr[i * 3 + 1], arr[i * 3 + 2]];
    at(pc.terrain, s).forEach((v, i) => expect(v).toBeCloseTo([6.4, -0.4, 0][i], 5));
    expect(pc.terrain[(s + 5) * 3 + 1]).toBeCloseTo(0.4, 5); // hi +10%
    at(pc.map, s).forEach((v, i) => expect(v).toBeCloseTo(at(pc.terrain, s)[i], 5)); // 지도 장면에서도 제자리
  });

  it('band가 없으면 점 구름은 예전과 같다', () => {
    const a = buildPointCloud(terrain, map, { noiseStride: 1, seed: 7 });
    const b = buildPointCloud(terrain, map, { noiseStride: 1, seed: 7, band: undefined });
    expect(Array.from(b.scatter)).toEqual(Array.from(a.scatter));
  });
});
```

`tests/unit/three-scenes.test.ts` 수정:

```ts
const KEYS: SceneKey[] = ['hero', 'about', 'problem', 'insight', 'bubble', 'validation', 'interval', 'limits', 'demo', 'stack', 'contact'];
```

`describe('SCENES', …)` 안 마지막에 추가:

```ts
  it('예측 구간 띠는 3-5(interval)에서만 보인다', () => {
    for (const k of KEYS) expect(SCENES[k].band).toBe(k === 'interval' ? 1 : 0);
  });
```

- [ ] **Step 2: 실패 확인**

Run: `npx vitest run tests/unit/three-data.test.ts tests/unit/three-scenes.test.ts`
Expected: FAIL (`dateIndex` 없음, `band` 필드 없음, `demo` 키 없음)

- [ ] **Step 3: `src/three/data.ts` 수정**

`mapSchema` 정의 다음에 추가:

```ts
// 3-5 챕터의 예측 구간 띠(export_demo.py가 만든다). 출발일마다 12개 노선·등급 조합의 lo·hi를
// 노선·등급 평균 대비 %×10으로 평균한 값이라 지형 높이와 같은 눈금이다.
export const bandSchema = z.object({
  asOf: z.string().regex(/^\d{4}-\d{2}-\d{2}$/),
  dates: z.array(z.string()).min(1),
  lo: ints,
  hi: ints,
}).refine((b) => b.lo.length === b.dates.length && b.hi.length === b.dates.length, 'band 배열 길이가 서로 다르다');
export type Band = z.infer<typeof bandSchema>;
```

`CURVE_X_JITTER` 상수 다음에 추가:

```ts
const BAND_STEPS = 6; // 출발일마다 lo~hi 사이에 찍는 점 수. 점 하나로는 "구간"이 아니라 점으로 읽힌다
const DAY_MS = 86_400_000;

function isoTime(iso: string): number {
  return Date.parse(`${iso}T00:00:00Z`);
}

export function daysBetween(fromIso: string, toIso: string): number {
  return Math.round((isoTime(toIso) - isoTime(fromIso)) / DAY_MS);
}

// 날짜 → 지형 출발일 축의 (소수) 번호. 지형 날짜는 가까운 쪽은 매일, 먼 쪽은 주 1회라 간격이 고르지 않아,
// 이웃한 두 날짜 사이를 날수로 선형 보간한다. 범위 밖이면 null(그릴 자리가 없다).
export function dateIndex(dates: string[], iso: string): number | null {
  const t = isoTime(iso);
  for (let i = 0; i + 1 < dates.length; i++) {
    const a = isoTime(dates[i]), b = isoTime(dates[i + 1]);
    if (t >= a && t <= b) return i + (b === a ? 0 : (t - a) / (b - a));
  }
  return null;
}
```

`buildPointCloud`의 시그니처와 앞부분을 바꾼다.

찾기:

```ts
export function buildPointCloud(t: Terrain, m: MapData, opts: { noiseStride: number; seed?: number }): PointCloud {
  // 신호 → 잡음 → 제거 → 예약 곡선 순서로 한 배열에 담는다(Points 하나로 그리기 위해).
  const rows: { dtd: number; date: number; pct: number; kind: number; weight: number }[] = [];
```

바꾸기:

```ts
export function buildPointCloud(t: Terrain, m: MapData, opts: { noiseStride: number; seed?: number; band?: Band }): PointCloud {
  // 신호 → 잡음 → 제거 → 예약 곡선 → 예측 구간 띠 순서로 한 배열에 담는다(Points 하나로 그리기 위해).
  // 띠를 맨 뒤에 두어, 띠가 있든 없든 앞 레이어들이 쓰는 난수 순서(= 캡처 이미지)가 바뀌지 않게 한다.
  const rows: { dtd: number; date: number; pct: number; kind: number; weight: number; pos?: [number, number, number] }[] = [];
```

예약 곡선을 넣는 `for` 루프(`for (let i = 0; i < t.curve.pct.length; i++) { … }`) **바로 다음**에 추가:

```ts
  // 예측 구간 띠(kind 4, 3-5 전용): x는 기준일 시점의 예약 일수라 지형 데이터가 끝나는 앞 가장자리("오늘")에 선다
  if (opts.band) {
    const b = opts.band;
    b.dates.forEach((d, i) => {
      const di = dateIndex(t.dates, d);
      if (di === null) return;
      const dtd = daysBetween(b.asOf, d);
      for (let k = 0; k < BAND_STEPS; k++) {
        const pct = b.lo[i] + ((b.hi[i] - b.lo[i]) * k) / (BAND_STEPS - 1);
        rows.push({ dtd, date: -1, pct, kind: 4, weight: 1, pos: terrainPosition(dtd, di, pct, t.maxDtd, t.dates.length) });
      }
    });
  }
```

`rows.forEach((r, i) => {` 안에서 `if (r.kind === 3) {` 블록 **바로 앞**에 추가:

```ts
    if (r.kind === 4 && r.pos) {
      // 띠도 지도 목표 = 지형 목표로 둔다(지도 장면에서는 셰이더가 uBand로 숨긴다)
      out.terrain.set(r.pos, i * 3);
      out.map.set(r.pos, i * 3);
      const u = rand() * 2 - 1, th = rand() * Math.PI * 2, rr = 14 * Math.cbrt(rand());
      const s = Math.sqrt(1 - u * u);
      out.scatter.set([rr * s * Math.cos(th), rr * u, rr * s * Math.sin(th)], i * 3);
      return;
    }
```

`loadSceneData`를 바꾼다:

```ts
export async function loadSceneData(dataVersion: string, fetcher: typeof fetch = fetch) {
  const get = async (url: string) => {
    const res = await fetcher(url);
    if (!res.ok) throw new Error(`${url}: HTTP ${res.status}`);
    return res.json();
  };
  const [terrain, map, band] = await Promise.all([
    get(`/data/terrain.${dataVersion}.json`),
    get('/data/map.v1.json'),
    get(`/data/band.${dataVersion}.json`),
  ]);
  return { terrain: terrainSchema.parse(terrain), map: mapSchema.parse(map), band: bandSchema.parse(band) };
}
```

- [ ] **Step 4: `src/three/scenes.ts` 수정**

`SceneKey` 줄을 바꾼다:

```ts
export type SceneKey = 'hero' | 'about' | 'problem' | 'insight' | 'bubble' | 'validation' | 'interval' | 'limits' | 'demo' | 'stack' | 'contact';
```

`SceneState`의 `curve` 줄 다음에 추가:

```ts
  band: number;     // 예측 구간 띠 레이어(kind 4) 보이기 — 3-5(interval)에서만 1
```

`const base = …`를 바꾼다:

```ts
const base = { assemble: 1, map: 0, noise: 1, removed: 0, drop: 0, curve: 0, band: 0 };
```

`interval:` 줄을 바꾸고 그 위에 주석을 단다:

```ts
  // 3-5: 지형 앞 가장자리("오늘")에 기준일의 예측 구간 띠를 세운다. 비스듬히 내려다보는 시점은 계획 3 그대로
  interval: { ...base, camera: [-12, 5, 12], target: [0, 0.5, 0], band: 1 },
```

`limits:` 줄 다음에 추가:

```ts
  // 데모: 조작 화면이 주인공이라 지형은 멀리 물러나 잡음을 줄인다(stack과 같은 구도)
  demo: { ...base, camera: [0, 16, 30], target: [0, 0, 0], noise: 0.3 },
```

`src/three/activeScene.ts`의 `SECTION_KEYS`를 바꾼다:

```ts
const SECTION_KEYS = new Set(['hero', 'about', 'demo', 'stack', 'contact']); // 'case'는 챕터들이 대신한다
```

- [ ] **Step 5: `src/three/shaders.ts` 수정**

정점 셰이더에서 다음을 바꾼다.

`attribute float aKind;     // 0 신호, 1 잡음, 2 제거, 3 예약 곡선(인사이트 전용)` → `attribute float aKind;     // 0 신호, 1 잡음, 2 제거, 3 예약 곡선(3-2 전용), 4 예측 구간 띠(3-5 전용)`

`uniform float uCurve;` 다음 줄에 `uniform float uBand;` 추가.

크기 줄을 바꾼다:

찾기: `float size = aKind < 0.5 ? 1.0 : (aKind < 1.5 ? 0.7 : (aKind < 2.5 ? 1.1 : 1.3 * mix(0.4, 1.0, trust)));`
바꾸기: `float size = aKind < 0.5 ? 1.0 : (aKind < 1.5 ? 0.7 : (aKind < 2.5 ? 1.1 : (aKind < 3.5 ? 1.3 * mix(0.4, 1.0, trust) : 1.0)));`

알파 마지막 줄을 바꾼다:

찾기: `    else vAlpha = 0.95 * uCurve * mix(0.12, 1.0, trust);`
바꾸기:

```glsl
    else if (aKind < 3.5) vAlpha = 0.95 * uCurve * mix(0.12, 1.0, trust);
    // 예측 구간 띠: 3-5 장면에서만 uBand로 나타난다(지도·다른 장면에서는 0)
    else vAlpha = 0.85 * uBand;
```

조각 셰이더는 바꾸지 않는다(`vKind > 1.5`가 kind 4도 텍스트 색 `uText`로 칠한다. 띠를 파란 지형 점과 구별하고 새 색을 만들지 않는다).

- [ ] **Step 6: `src/three/TerrainPoints.tsx` 수정**

uniforms에서 `uCurve: { value: 0 },` 다음 줄에 `uBand: { value: 0 },` 추가. `step('uCurve', t.curve);` 다음 줄에 `step('uBand', t.band);` 추가.

- [ ] **Step 7: `src/three/TerrainScene.tsx` 수정**

import에서 `type MapData, type Terrain`에 `type Band`를 더한다: `import { buildPointCloud, loadSceneData, type Band, type MapData, type Terrain } from './data';`

`useState<{ terrain: Terrain; map: MapData } | null>(null)` → `useState<{ terrain: Terrain; map: MapData; band: Band } | null>(null)`

`buildPointCloud(data.terrain, data.map, { noiseStride: isPortrait ? 2 : 1, seed: 7 })` → `buildPointCloud(data.terrain, data.map, { noiseStride: isPortrait ? 2 : 1, seed: 7, band: data.band })`

- [ ] **Step 8: 통과 확인**

Run: `npx vitest run tests/unit/three-data.test.ts tests/unit/three-scenes.test.ts tests/unit/three-active.test.ts && npm run typecheck`
Expected: PASS

- [ ] **Step 9: 커밋**

```bash
git add src/three tests/unit/three-data.test.ts tests/unit/three-scenes.test.ts
git commit -m "feat: interval band layer on the terrain and a calm demo scene"
```

---

### Task 10: 3-5 챕터 문구와 대체 이미지 다시 캡처

**Files:**
- Modify: `content/{ko,en,ja}.json` (`case.interval.body3`, `figure.interval`)
- Modify: `src/components/sections/CaseStudy.tsx` (interval `paras: 3`)
- Modify: `public/fallback/*.webp` (다시 캡처)
- (필요하면) Modify: `src/three/scenes.ts` (interval 카메라)

- [ ] **Step 1: 문구 추가·수정**

`case.interval`의 `body2` 다음에 `body3`를 넣고, `figure.interval`을 바꾼다.

ko:
- `"body3": "지형 앞쪽 가장자리의 흰 띠가 기준일에 계산한 출발일별 예측 구간입니다. 아래 데모에서 날짜별로 확인할 수 있습니다."`
- `"interval": "가격 지형의 앞쪽 가장자리를 따라 출발일별 예측 구간이 흰 세로 띠로 이어진 모습"`

en:
- `"body3": "The white band along the front edge of the landscape is the prediction interval for each departure date, computed on the reference date. You can check it date by date in the demo below."`
- `"interval": "White vertical bands along the front edge of the price landscape, showing the prediction interval for each departure date"`

ja:
- `"body3": "地形の手前の縁に沿った白い帯が、基準日に計算した出発日ごとの予測区間です。下のデモで日付ごとに確認できます。"`
- `"interval": "価格地形の手前の縁に沿って、出発日ごとの予測区間が白い縦の帯として並ぶ様子"`

`src/components/sections/CaseStudy.tsx`: `{ id: 'interval', gate: 'GATE 05 — INTERVAL', paras: 2 },` → `paras: 3`

- [ ] **Step 2: 빌드와 캡처**

```bash
npm run build && npm run fallbacks
```

Expected: `fallback/hero.webp` … `fallback/interval.webp` 다섯 줄. 띠는 난수 순서상 맨 뒤라 hero·problem·insight·bubble은 픽셀이 거의 같아야 한다(`git diff --stat public/fallback`로 확인. 크기가 크게 바뀌면 원인을 찾는다).

- [ ] **Step 3: interval 이미지 눈 확인**

`public/fallback/interval.webp`를 연다(Read 도구로 이미지 보기). 흰 세로 띠가 지형 앞 가장자리를 따라 **분명히 보여야** 한다. 띠가 지형 점에 묻혀 안 보이거나 화면 밖이면 `src/three/scenes.ts`의 interval 카메라를 아래 값으로 바꾸고 Step 2~3을 다시 한다. 띠는 x 약 −1.8~7.7(예약 일수 90~3일)에 있으므로 출발 당일 쪽(+x)에서 앞 가장자리를 비스듬히 보는 구도다.

```ts
  interval: { ...base, camera: [16, 7, 10], target: [3, 0.5, 0], band: 1 },
```

이 값으로도 안 보이면 멈추고 사용자에게 캡처 이미지를 보여 준 뒤 구도를 정한다. 세로 화면(Pixel 7)도 `npm run dev`에서 개발자 도구 기기 모드로 3-5 챕터까지 스크롤해 띠가 글 카드 위쪽에 보이는지 확인한다.

- [ ] **Step 4: 테스트**

Run: `npm test`
Expected: PASS (content 키 일치, 숫자 금지 포함)

- [ ] **Step 5: 커밋**

```bash
git add content/ src/components/sections/CaseStudy.tsx public/fallback src/three/scenes.ts
git commit -m "feat: explain the interval band in chapter 3-5 and recapture fallbacks"
```

---

### Task 11: e2e — 데모 조작·실패 경로·접근성·청크 분리

**Files:**
- Create: `tests/e2e/demo.spec.ts`
- Modify: `tests/e2e/site.spec.ts`

- [ ] **Step 1: `tests/e2e/site.spec.ts` 수정**

`for (const id of ['about', 'case', 'stack', 'contact'])` → `for (const id of ['about', 'case', 'demo', 'stack', 'contact'])`

`content` 타입에 데모 안내를 더한다. 찾기:

```ts
) as Record<(typeof PAGES)[number]['lang'], { case: { heading: string }; contact: { emailFallback: string } }>;
```

바꾸기:

```ts
) as Record<(typeof PAGES)[number]['lang'], { case: { heading: string }; contact: { emailFallback: string }; demo: { noJs: string } }>;
```

`JS 없이` describe 안 테스트의 마지막 `expect` 다음 줄에 추가:

```ts
      await expect(page.getByText(content[lang].demo.noJs)).toBeVisible();
```

- [ ] **Step 2: `tests/e2e/demo.spec.ts` 작성**

```ts
// 데모 e2e: 처음 조합, 키보드·누르기 조작, 노선·등급 바꾸기, 조작 뒤 알림, 불러오기 실패 후 다시 시도,
// 움직임 줄이기, 세 언어 axe, 데이터 모듈(zod)이 초기 청크에 없는지.
import AxeBuilder from '@axe-core/playwright';
import { expect, test, type Page } from '@playwright/test';
import { readFileSync, readdirSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

// site.spec.ts와 같은 이유로 JSON은 fs로 읽는다(Playwright TS 로더의 JSON import 제약)
const read = (p: string) => JSON.parse(readFileSync(fileURLToPath(new URL(p, import.meta.url)), 'utf-8'));
const facts = read('../../data/facts.json') as { demoDefault: { route: string; cabin: string; date: string } };
const ko = read('../../content/ko.json') as { demo: { routeLabel: string; error: string; retry: string } };

async function openDemo(page: Page, path = '/') {
  await page.goto(path);
  await page.locator('#demo').scrollIntoViewIfNeeded();
  const slider = page.getByRole('slider');
  await expect(slider).toBeVisible({ timeout: 15_000 });
  return slider;
}

test('처음에는 facts.demoDefault 조합(DROP EXPECTED)이 선택돼 있다', async ({ page }) => {
  await openDemo(page);
  await expect(page.getByLabel(ko.demo.routeLabel)).toHaveValue(facts.demoDefault.route);
  await expect(page.locator('.demo-cabin')).toHaveText(facts.demoDefault.cabin);
  await expect(page.locator('.demo-result .badge')).toHaveText('DROP EXPECTED');
});

test('키보드: Home·→·End로 날짜를 고른다', async ({ page }) => {
  const slider = await openDemo(page);
  await slider.focus();
  await page.keyboard.press('Home');
  await expect(slider).toHaveAttribute('aria-valuenow', (await slider.getAttribute('aria-valuemin'))!);
  const first = (await slider.getAttribute('aria-valuetext'))!;
  await page.keyboard.press('ArrowRight');
  await expect(slider).not.toHaveAttribute('aria-valuetext', first);
  await page.keyboard.press('End');
  await expect(slider).toHaveAttribute('aria-valuenow', (await slider.getAttribute('aria-valuemax'))!);
});

test('막대 왼쪽 끝을 누르면 첫 날짜가 선택된다', async ({ page }) => {
  const slider = await openDemo(page);
  const box = (await slider.boundingBox())!;
  await page.mouse.click(box.x + 1, box.y + box.height / 2);
  await expect(slider).toHaveAttribute('aria-valuenow', (await slider.getAttribute('aria-valuemin'))!);
});

test('노선과 등급을 바꾸면 선택지와 막대가 새로 그려진다', async ({ page }) => {
  const slider = await openDemo(page);
  const before = (await slider.getAttribute('aria-valuetext'))!;
  const other = facts.demoDefault.route === 'ICN_KIX' ? 'ICN_NRT' : 'ICN_KIX';
  await page.getByLabel(ko.demo.routeLabel).selectOption(other);
  await page.locator('.demo-cabin').click();
  await expect(page.locator('.demo-cabin')).toHaveText(facts.demoDefault.cabin === 'LCC' ? 'FSC' : 'LCC');
  await expect(slider).not.toHaveAttribute('aria-valuetext', before);
});

test('조작한 뒤 결과를 aria-live로 한 번 알린다', async ({ page }) => {
  const slider = await openDemo(page);
  const live = page.locator('#demo [aria-live="polite"]');
  await expect(live).toHaveText(''); // 처음 표시 때는 알리지 않는다
  await slider.focus();
  // 처음 날짜가 이미 끝이면 End나 Home 중 하나는 제자리라 조작으로 치지 않는다 → 둘 다 눌러 한 번은 바뀌게 한다
  await page.keyboard.press('End');
  await page.keyboard.press('Home');
  await expect(live).toContainText(/BUY NOW|DROP EXPECTED|WAIT/);
  // 알림은 지금 화면의 배지와 같은 결과여야 한다
  await expect(live).toContainText((await page.locator('.demo-result .badge').textContent())!);
});

test('데모 데이터를 못 받으면 안내가 뜨고, 다시 시도하면 불러온다', async ({ page }) => {
  let fail = true;
  await page.route('**/data/demo.*.json', (r) => (fail ? r.fulfill({ status: 500, body: 'x' }) : r.continue()));
  await page.goto('/');
  await page.locator('#demo').scrollIntoViewIfNeeded();
  await expect(page.getByRole('alert')).toContainText(ko.demo.error, { timeout: 15_000 });
  fail = false;
  await page.getByRole('button', { name: ko.demo.retry }).click();
  await expect(page.getByRole('slider')).toBeVisible();
});

test.describe('움직임 줄이기', () => {
  test.use({ reducedMotion: 'reduce' });
  test('데모가 그대로 동작한다', async ({ page }) => {
    const slider = await openDemo(page);
    await slider.focus();
    await page.keyboard.press('End');
    await expect(slider).toHaveAttribute('aria-valuenow', (await slider.getAttribute('aria-valuemax'))!);
    await expect(page.locator('.demo-result .badge')).toBeVisible();
  });
});

for (const path of ['/', '/en/', '/ja/']) {
  test(`${path} 데모를 불러온 뒤 axe 위반 없음`, async ({ page }) => {
    await openDemo(page, path);
    const result = await new AxeBuilder({ page }).include('#demo').withTags(['wcag2a', 'wcag2aa', 'wcag21aa']).analyze();
    expect(result.violations).toEqual([]);
  });
}

test('초기 HTML(세 언어)은 데모 데이터 모듈(zod 포함) 청크를 직접 불러오지 않는다', async () => {
  const dir = 'out/_next/static/chunks';
  // src/demo/source.ts의 검사 메시지 머리말. 이 문자열이 든 청크가 데이터 모듈 청크다
  const marker = 'demo-series-length-mismatch';
  const chunks = readdirSync(dir).filter((f) => f.endsWith('.js') && readFileSync(`${dir}/${f}`, 'utf8').includes(marker));
  expect(chunks.length).toBeGreaterThan(0);
  for (const file of ['out/index.html', 'out/en/index.html', 'out/ja/index.html']) {
    const html = readFileSync(file, 'utf8');
    for (const f of chunks) expect(html, file).not.toContain(f);
  }
});
```

- [ ] **Step 3: 실행**

Run: `npm run build && npm run e2e`
Expected: desktop·mobile 두 프로젝트 모두 PASS(기존 site·terrain 테스트 포함).

실패하면 `superpowers:systematic-debugging`으로 원인을 찾는다. 자주 나올 수 있는 경우:
- `막대 왼쪽 끝` 테스트에서 첫 막대가 예측 없는 날이면 `nearestSelectable`이 첫 고를 수 있는 날로 옮긴다. 그래도 `aria-valuemin`과 같아야 정상이다
- axe `color-contrast` 위반: 해당 요소 색을 `--mute`(대비 4.5:1 보장)나 `--tx`로 바꾼다. 색을 새로 만들지 않는다
- `청크` 테스트에서 `chunks.length`가 0이면 빌드가 문자열을 바꾼 것이다: `grep -l demo-series out/_next/static/chunks/*.js`로 확인한다

- [ ] **Step 4: 커밋**

```bash
git add tests/e2e/demo.spec.ts tests/e2e/site.spec.ts
git commit -m "test: e2e for demo keyboard/pointer, failure retry, live region, axe, lazy data chunk"
```

---

### Task 12: 문서 갱신, 전체 검증, PR

**Files:**
- Modify: `README.md`, `CLAUDE.md`, `docs/superpowers/specs/2026-09-23-portfolio-design.md`

- [ ] **Step 1: README 갱신**

`npm run map        # 지도 데이터 추출 (export_map.py)` 다음 줄에 추가:

```
npm run demo       # 데모 예측·예측 구간 띠 추출 (export_demo.py, 모델을 돌려 몇 분)
npm run pytest     # 데이터 스크립트 테스트
```

`**재학습 후 업데이트 순서:** \`npm run facts\` → \`npm run terrain\` → \`npm run build\` → \`npm run fallbacks\` → commit` → `**재학습 후 업데이트 순서:** \`npm run facts\` → \`npm run terrain\` → \`npm run demo\` → \`npm run build\` → \`npm run fallbacks\` → commit (데이터 스크립트의 파이썬은 \`scripts/py.sh\`가 고른다)`

- [ ] **Step 2: 스펙 갱신** — `docs/superpowers/specs/2026-09-23-portfolio-design.md`

§5.3 "구현 결과" 문단의 `예측 구간 보정 챕터의 지형(3-5 interval)` → `예측 구간 보정 챕터의 지형과 앞 가장자리의 예측 구간 띠(3-5 interval, 계획 2)`. 같은 문단의 재학습 순서를 README와 같게 `npm run facts` → `npm run terrain` → `npm run demo` → `npm run build` → `npm run fallbacks`로 바꾼다.

§6.2 끝에 추가:

```markdown
**구현 결과 (계획 2, 2026-09-24)**

- `scripts/export_demo.py` → `public/data/demo.<기준일>.json`(88일 × 12조합, 열 방향 배열 `price`/`lo`/`hi`/`reco`, 대표 편 없는 조합은 `null`), `public/data/band.<기준일>.json`(3-5 띠), `facts.demoDefault`.
- 이유 코드: `{action, why, bestDay, bestPrice, waitDays, saving, savingPct, globalBestDay, confidence}`. `why`는 `IMMINENT`·`PAST_OPTIMAL`·`AT_LOW`·`SMALL_SAVING`(BUY_NOW), `FALLING`(DROP_EXPECTED), `LATER_LOW`(WAIT). `savingPct`는 "지금보다 몇 % 싼가"라 양수다(위 예시의 −8.7은 같은 값을 변화율로 적은 것).
- 확신도는 모델이 `높음`/`보통` 두 단계만 내므로 `high`/`medium`만 있다(대기·하락 예상은 절약률이 8% 이상이라 항상 둘 중 하나).
- 공휴일 코드는 workalendar 영어 이름에서 만든 `kr_…`/`jp_…`이고, 이름표는 `content/*.json`의 `demo.holidays`에 있다.
```

§3 표의 3-5 줄 `q10~q90 띠 연출 (띠는 모델 예측 구간이 필요해 계획 2(데모 데이터)로 옮김. 계획 3에서는 지형을 비스듬히 보는 장면만)` → `q10~q90 띠 연출 (계획 2에서 구현: 지형 앞 가장자리에 기준일의 출발일별 예측 구간 띠)`

- [ ] **Step 3: CLAUDE.md 갱신**

진행 상태 표의 계획 2 줄 → `| 계획 2: 데모 | ✅ 구현 완료 2026-09-24 · branch \`plan-2-demo\` · **PR로 CI 확인 후 main 병합 대기** |`

`### 다음 세션 할 일` 목록의 3번 항목(`**계획 2(데모) 계획서 작성 → 실행**` 로 시작하는 블록)을 한 줄로 줄인다: `3. ✅ **계획 2(데모)** — 계획서 \`docs/superpowers/plans/2026-09-24-plan-2-demo.md\`. 남은 일: PR CI 확인 → main 병합 → Vercel 배포 확인. 한국어 데모 문구 사용자 검토, 영·일 검수(스펙 §14)`

"다른 기기(맥북)에서 이어서 작업하기"의 `git switch plan-3-terrain` → `git switch main`(또는 진행 중인 브랜치)

- [ ] **Step 4: 전체 검증** — 모두 새로 실행하고 출력을 확인한다(`superpowers:verification-before-completion`)

```bash
npm run typecheck && npm test && npm run pytest && npm run build && npm run e2e
```

Expected: 모두 성공. 하나라도 실패하면 고친 뒤 처음부터 다시 돌린다.

용량도 확인한다:

```bash
gzip -c public/data/demo.2026-09-22.json | wc -c   # 512000 이하
gzip -c public/data/band.2026-09-22.json | wc -c   # 51200 이하
```

- [ ] **Step 5: 커밋과 PR**

```bash
git add README.md CLAUDE.md docs/superpowers/specs/2026-09-23-portfolio-design.md
git commit -m "docs: record plan 2 demo implementation and data refresh order"
git push -u origin plan-2-demo
gh pr create --base main --head plan-2-demo --title "Plan 2: interactive demo" --body "$(cat <<'EOF'
## 요약
- 미리 계산한 예측(demo.2026-09-22.json)으로 동작하는 데모 섹션: 질문 문장 선택지, 출발일 막대(slider), 예측가·80% 구간·추천·이유
- ForecastSource 인터페이스 + StaticForecastSource(데모 섹션 근처에서 dynamic import)
- 3-5 챕터 지형 앞 가장자리에 예측 구간 띠, 대체 이미지 다시 캡처
- 이유 문장은 코드+값으로 저장하고 3개 언어 틀로 조립

## 확인
- typecheck, vitest, pytest, build, Playwright(desktop·mobile, axe 포함) 통과

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
gh pr checks --watch
```

Expected: CI 통과. **main 병합은 사용자에게 묻고 한다**(병합하면 Vercel 운영 배포가 바로 된다).

# SIGNAL 계획 4-2/4 — 연출과 남은 성능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스펙 §4의 모션(로딩 화면 플립 카운터, 플립 글자판, 단어 단위 텍스트 리빌, Lenis 부드러운 스크롤 + GSAP ScrollTrigger)을 성능 목표를 해치지 않게 넣고, 운영 성능 87을 90 이상으로 올리며, 휴대폰 3-5 예측 구간 띠를 읽히게 만든다.

**Architecture:** 페이지 머리의 작은 인라인 부트 스크립트가 첫 그리기 전에 `<html data-3d="pending">`과 로딩 화면 생략 여부를 정한다. 3D 판정이 끝날 때까지 대체 이미지를 그리지 않아, 3D를 쓸 방문자에게는 첫 화면 이미지가 LCP가 되지 않고 받지도 않는다(LCP가 이름 글자로 앞당겨진다). 로딩 화면은 서버 HTML + CSS 애니메이션만으로 1.2초 뒤 사라지고(JS가 없거나 늦어도 막히지 않음), 숫자 플립은 작은 클라이언트 컴포넌트가 얹는다. GSAP·ScrollTrigger·Lenis는 `src/motion/run.ts`에 모아 첫 그리기 뒤 `import()`로 불러오며(초기 JS 150KB 유지, `npm run size`가 지킴), 움직임 줄이기에서는 아예 불러오지 않는다. 플립 글자판은 GSAP 없이 `requestAnimationFrame`만 쓰는 `src/motion/flip.ts` 하나로 로딩 카운터·GATE 제목·데모 예측가가 함께 쓴다.

**Tech Stack:** Next.js 16.3 static export, React 19, gsap 3.15(ScrollTrigger), lenis 1.3, Vitest 5, Playwright 1.63, Lighthouse 13

**Spec:** `docs/superpowers/specs/2026-09-23-portfolio-design.md` §3(0 로딩 화면), §4(모션 규칙), §8.1·§8.2(로딩 순서·목표), §9(움직임 줄이기·스크린리더)

**선행:** 계획 1·2·3·4-1 main 병합. 운영 측정(4-1 뒤): 성능 ko 87 · en 87 · ja 86, LCP 2.5~2.8s(LCP 요소는 첫 화면 대체 이미지), CLS 0, TBT 300~370ms. **공개 전환(`LAUNCHED`)은 이 계획에 넣지 않는다.**

## Global Constraints

- 모션 규칙(스펙 §4): 플립 글자판 글자당 약 40ms·전체 0.8초 이내, 텍스트 리빌 단어 단위 아래에서 떠오름·0.9초·단어당 60ms, 이징은 `cubic-bezier(.16,1,.3,1)`(GSAP `expo.out`) 하나, bounce 금지
- 로딩 화면 `LOADING 242,874 ROWS`: 최대 1.2초 고정 연출, 실제 로딩 진행률과 연결하지 않음, 같은 세션 재방문·움직임 줄이기·JS 없음에서는 생략, 클릭을 막지 않음(`pointer-events: none`), 레이아웃을 밀지 않음(fixed). 숫자는 `facts.data.filteredRows`에서(문구에 숫자 직접 기입 금지 규칙 유지)
- 움직임 줄이기: 로딩 화면·플립·리빌·Lenis·GSAP 모두 끔(최종 값만 보임). 3D는 기존대로 대체 이미지
- 첫 화면 이름(`h1`)과 첫 화면 글에는 리빌을 걸지 않는다 — LCP 요소라 투명에서 시작하면 LCP가 늦어진다
- 초기 JS gzip ≤ 150KB(세 언어), `npm run size` 통과. gsap·lenis는 초기 청크에 들어가면 안 된다(클라이언트 import 경계 테스트 + size)
- 스크린리더: 플립은 완성값을 `sr-only`로 읽히고 움직이는 글자는 `aria-hidden`(스펙 §9.3), 로딩 화면은 `aria-hidden`
- 색·글꼴 기존 토큰만. 한국어 주석(파일 머리 역할 + "왜"). 커밋 noreply + Co-Authored-By. 브랜치 → PR → CI → **사용자가 미리보기로 연출을 보고 승인한 뒤** 병합
- 사용자와의 대화는 한국어

## Review Focus

1. **LCP·CLS 회귀 없음**: 첫 화면 이름이 LCP, 로딩 화면이 레이아웃을 밀지 않음 → Task 1 e2e(첫 화면 이미지 요청 없음), 기존 CLS e2e, Task 7 Lighthouse
2. **JS 없음·움직임 줄이기·데이터 실패**에서 콘텐츠가 전부 보임 → Task 1·3·6 e2e
3. **초기 JS 150KB** → `npm run size`
4. **스크린리더**가 플립 중간 글자를 읽지 않음 → Task 2 단위 테스트 + Task 6 e2e
5. **판정 대기가 영원히 남지 않음**(3D 코드 실패 시) → Task 1 단위 테스트(6초 뒤 off)

---

## File Structure

```
src/lib/boot.ts                    (신규) 첫 그리기 전 인라인 스크립트(판정 대기, 로딩 화면 생략)
src/components/RootDocument.tsx    <head>에 부트 스크립트·noscript 스타일, suppressHydrationWarning
src/components/sections/{Hero,ChapterFigure}.tsx   첫 화면 이미지 우선 로딩 되돌림
src/motion/flip.ts                 (신규) 플립 글자판(rAF, GSAP 없음)
src/motion/words.ts                (신규) 리빌용 단어 나누기(순수)
src/motion/run.ts                  (신규) GSAP·ScrollTrigger·Lenis 시작/정리(지연 로딩 전용)
src/components/Motion.tsx          (신규, 'use client') 첫 그리기 뒤 run.ts 불러오기
src/components/Loader.tsx          (신규, 'use client') 로딩 화면 + 카운터 플립
src/components/HomePage.tsx        Loader·Motion 끼우기
src/components/sections/*.tsx      h2에 data-reveal, GATE 제목에 data-flip-on-enter
src/components/demo/DemoResult.tsx 예측가 플립
src/three/data.ts, shaders.ts      3-5 띠: 지형 출발일과 맞는 날만, 점 크기
src/styles/globals.css             판정 대기, 로딩 화면, 리빌 단어
tests/unit/{boot,flip,words}.test.ts, three-data.test.ts   단위
tests/e2e/motion.spec.ts           (신규) 로딩 화면·리빌·플립·움직임 줄이기
package.json                       gsap, lenis
public/fallback/interval.webp      다시 캡처
```

---

### Task 1: 3D 판정 대기 — 첫 화면 이미지를 LCP에서 빼기

**Files:**
- Create: `src/lib/boot.ts`, `tests/unit/boot.test.ts`
- Modify: `src/components/RootDocument.tsx`, `src/styles/globals.css`, `src/components/sections/Hero.tsx`, `src/components/sections/ChapterFigure.tsx`, `tests/e2e/terrain.spec.ts`

**배경:** 지금은 JS가 3D를 켜기 전까지 대체 이미지가 보인다. 3D를 쓸 방문자에게는 곧 숨겨질 첫 화면 이미지(59KB)가 LCP 요소가 되고(운영 LCP 2.5~2.8s), 계획 4-1에서 우선 로딩까지 걸어 두었다. 첫 그리기 전에 "판정 대기"를 걸어 이미지를 숨기면 LCP가 이름 글자(FCP 1.2~1.5s 무렵)로 앞당겨진다. 움직임 줄이기·JS 없음에서는 대기를 걸지 않아 이미지가 곧바로 보인다. 3D 코드가 실패해 판정이 안 나도 6초 뒤 대체 화면으로 넘어간다.

- [ ] **Step 1: 브랜치**

```bash
cd "$(git rev-parse --show-toplevel)"
git switch main && git pull --ff-only && git switch -c plan-4-2-motion
```

- [ ] **Step 2: 실패하는 테스트** — `tests/unit/boot.test.ts`

```ts
// 부트 스크립트 검사: 실제로 <head>에 들어가는 문자열(BOOT_SCRIPT)을 가짜 window로 실행해 본다.
import { describe, expect, it, vi } from 'vitest';
import { BOOT_SCRIPT, PENDING_TIMEOUT_MS } from '@/lib/boot';

function fakeWindow({ reduced = false, search = '', visited = false, storageThrows = false } = {}) {
  const attrs = new Map<string, string>();
  const classes = new Set<string>();
  const store = new Map<string, string>(visited ? [['signal.loaded', '1']] : []);
  const timers: (() => void)[] = [];
  const win = {
    document: { documentElement: {
      setAttribute: (k: string, v: string) => attrs.set(k, v),
      getAttribute: (k: string) => attrs.get(k) ?? null,
      classList: { add: (c: string) => classes.add(c) },
    } },
    matchMedia: () => ({ matches: reduced }),
    location: { search },
    sessionStorage: {
      getItem: (k: string) => { if (storageThrows) throw new Error('blocked'); return store.get(k) ?? null; },
      setItem: (k: string, v: string) => { store.set(k, v); },
    },
    setTimeout: (fn: () => void, ms: number) => { expect(ms).toBe(PENDING_TIMEOUT_MS); timers.push(fn); },
  };
  new Function('window', BOOT_SCRIPT)(win);
  return { attrs, classes, store, runTimers: () => timers.forEach((f) => f()) };
}

describe('BOOT_SCRIPT', () => {
  it('처음 방문: 로딩 화면을 보이고 방문 표시, 3D 판정 대기', () => {
    const w = fakeWindow();
    expect(w.classes.has('no-loader')).toBe(false);
    expect(w.store.get('signal.loaded')).toBe('1');
    expect(w.attrs.get('data-3d')).toBe('pending');
  });
  it('같은 세션 재방문이면 로딩 화면 생략', () => {
    expect(fakeWindow({ visited: true }).classes.has('no-loader')).toBe(true);
  });
  it('움직임 줄이기: 로딩 화면 생략, 판정 대기 없음(대체 이미지가 곧바로 보인다)', () => {
    const w = fakeWindow({ reduced: true });
    expect(w.classes.has('no-loader')).toBe(true);
    expect(w.attrs.has('data-3d')).toBe(false);
  });
  it('캡처 모드(?capture=)에서는 로딩 화면 생략', () => {
    expect(fakeWindow({ search: '?capture=hero' }).classes.has('no-loader')).toBe(true);
  });
  it('판정이 끝나지 않으면 제한 시간 뒤 off(대체 화면)', () => {
    const w = fakeWindow();
    w.runTimers();
    expect(w.attrs.get('data-3d')).toBe('off');
  });
  it('이미 on이 됐으면 제한 시간이 지나도 그대로', () => {
    const w = fakeWindow();
    w.attrs.set('data-3d', 'on');
    w.runTimers();
    expect(w.attrs.get('data-3d')).toBe('on');
  });
  it('저장소가 막혀 있어도 멈추지 않고 로딩 화면만 생략', () => {
    const w = fakeWindow({ storageThrows: true });
    expect(w.classes.has('no-loader')).toBe(true);
    expect(w.attrs.get('data-3d')).toBe('pending');
  });
});
```

Run: `npx vitest run tests/unit/boot.test.ts` / Expected: FAIL(모듈 없음)

- [ ] **Step 3: `src/lib/boot.ts`**

```ts
// 첫 그리기 전에 <head>에서 실행되는 인라인 스크립트. React가 뜨기 전에 두 가지를 정한다.
// 1) 로딩 화면 생략(같은 세션 재방문·움직임 줄이기·캡처 모드) → <html class="no-loader">
// 2) 3D 판정 대기(<html data-3d="pending">) → 대체 이미지를 숨겨, 곧 3D로 바뀔 첫 화면 이미지가
//    LCP가 되거나 받아지지 않게 한다. 움직임 줄이기면 3D를 안 쓰므로 대기 없이 이미지를 곧바로 보인다.
// 문자열로 넣으므로 옛 문법만 쓴다(var, function). tests/unit/boot.test.ts가 이 문자열을 그대로 실행해 본다.
export const PENDING_TIMEOUT_MS = 6000; // 3D 코드가 실패해 판정이 끝나지 않아도 이 시간 뒤에는 대체 화면을 보인다

export const BOOT_SCRIPT = `(function (w) {
  var d = w.document.documentElement;
  var reduced = false;
  try { reduced = w.matchMedia('(prefers-reduced-motion: reduce)').matches; } catch (e) {}
  try {
    if (reduced || /[?&]capture=/.test(w.location.search) || w.sessionStorage.getItem('signal.loaded')) d.classList.add('no-loader');
    else w.sessionStorage.setItem('signal.loaded', '1');
  } catch (e) { d.classList.add('no-loader'); }
  if (!reduced) {
    d.setAttribute('data-3d', 'pending');
    w.setTimeout(function () { if (d.getAttribute('data-3d') === 'pending') d.setAttribute('data-3d', 'off'); }, ${PENDING_TIMEOUT_MS});
  }
})(window);`;
```

Run: `npx vitest run tests/unit/boot.test.ts` / Expected: PASS

- [ ] **Step 4: `<head>`에 넣기** — `src/components/RootDocument.tsx`

import 추가: `import { BOOT_SCRIPT } from '@/lib/boot';`

`<html …>` 여는 태그에 `suppressHydrationWarning`을 더하고(부트 스크립트가 React보다 먼저 `data-3d`·`class`를 바꾸므로), `<body>` 앞에 넣는다:

```tsx
      <head>
        {/* 첫 그리기 전에 3D 판정 대기·로딩 화면 생략을 정한다(src/lib/boot.ts) */}
        <script dangerouslySetInnerHTML={{ __html: BOOT_SCRIPT }} />
        {/* JS가 없으면 로딩 화면을 아예 보이지 않는다 */}
        <noscript><style>{'.loader{display:none}'}</style></noscript>
      </head>
```

- [ ] **Step 5: CSS와 첫 화면 이미지** — `src/styles/globals.css`

`html[data-3d="on"] .scene-figure { display: none; }` 줄을 바꾼다:

```css
/* 3D가 켜졌거나 판정 대기 중이면 대체 이미지를 숨긴다. 대기 중에 보이면 곧 사라질 이미지가 LCP가 되고 받아진다 */
html:is([data-3d="on"], [data-3d="pending"]) .scene-figure { display: none; }
```

`src/components/sections/Hero.tsx`: `<ChapterFigure locale={locale} sceneKey="hero" priority />` → `<ChapterFigure locale={locale} sceneKey="hero" />`. `src/components/sections/ChapterFigure.tsx`에서 `priority` prop과 `loading="eager"`/`fetchPriority` 분기를 지우고 모든 이미지를 `loading="lazy"`로 되돌린다(숨겨진 lazy 이미지는 받지 않는다 — 대기 중에 받지 않는 것이 목적). 파일 주석도 그에 맞게 고친다.

- [ ] **Step 6: e2e** — `tests/e2e/terrain.spec.ts`에 추가:

```ts
test('3D가 켜지는 환경에서는 첫 화면 대체 이미지를 받지 않는다(LCP는 이름 글자)', async ({ page }) => {
  const hero: string[] = [];
  page.on('request', (r) => { if (r.url().includes('/fallback/hero.webp')) hero.push(r.url()); });
  await page.goto('/');
  await expect(page.locator('html')).toHaveAttribute('data-3d', 'on', { timeout: 20_000 });
  expect(hero).toEqual([]);
});
```

- [ ] **Step 7: 확인**

Run: `npm run typecheck && npm test && npm run build && npm run size && npx playwright test tests/e2e/terrain.spec.ts tests/e2e/site.spec.ts`
Expected: 모두 PASS. 기존 "JS 없이 대체 이미지 다섯 장", "움직임 줄이기에서 대체 이미지", "지형 데이터 실패 → 대체 화면", "3D 켜져도 첫 화면 이름 영역 고정(CLS)"이 모두 그대로 통과해야 한다. 빌드 뒤 `npm run lighthouse`로 재서 LCP 요소와 점수를 기록한다(로컬은 낮게 나오므로 전후 비교만).

- [ ] **Step 8: 커밋**

```bash
git add src/lib/boot.ts tests/unit/boot.test.ts src/components/RootDocument.tsx src/styles/globals.css src/components/sections/Hero.tsx src/components/sections/ChapterFigure.tsx tests/e2e/terrain.spec.ts docs/superpowers/plans/2026-09-24-plan-4-2-motion.md
git commit -m "perf: hold fallback images until the 3D decision so the hero name is the LCP"
```

---

### Task 2: 플립 글자판 모듈과 데모 예측가

**Files:**
- Create: `src/motion/flip.ts`, `tests/unit/flip.test.ts`
- Modify: `src/components/demo/DemoResult.tsx`

- [ ] **Step 1: 실패하는 테스트** — `tests/unit/flip.test.ts`

```ts
// 플립 글자판의 순수 계산: 글자별 자리 잡는 시각(글자당 40ms, 전체 0.8초 이내)과 중간 프레임.
import { describe, expect, it } from 'vitest';
import { FLIP, frameAt, settleTimes } from '@/motion/flip';

describe('settleTimes', () => {
  it('글자당 40ms씩 늦게 자리 잡는다', () => {
    expect(settleTimes(3)).toEqual([40, 80, 120]);
  });
  it('글자가 많으면 간격을 줄여 전체가 0.8초를 넘지 않는다', () => {
    const t = settleTimes(40);
    expect(t.at(-1)).toBeLessThanOrEqual(FLIP.maxMs);
    expect(t[1] - t[0]).toBeLessThan(FLIP.charMs);
  });
  it('빈 문자열', () => expect(settleTimes(0)).toEqual([]));
});

describe('frameAt', () => {
  const zero = () => 0; // 무작위 자리를 늘 첫 후보로
  it('자리 잡은 글자는 최종 글자, 아직이면 같은 종류의 다른 글자', () => {
    expect(frameAt('A1', 50, [40, 80], zero)).toBe('A0');
    expect(frameAt('A1', 100, [40, 80], zero)).toBe('A1');
  });
  it('숫자·영문 대문자만 섞고 나머지(쉼표·공백·한글·기호)는 그대로', () => {
    expect(frameAt('9,4 원₩', 0, [10, 10, 10, 10, 10, 10], zero)).toBe('0,0 원₩');
  });
});
```

Run: `npx vitest run tests/unit/flip.test.ts` / Expected: FAIL

- [ ] **Step 2: `src/motion/flip.ts`**

```ts
// 플립 글자판(스펙 §4): 숫자·영문 대문자가 잠깐 뒤섞였다가 앞에서부터 차례로 최종 글자에 자리 잡는다.
// GSAP 없이 requestAnimationFrame만 써서 초기 JS에 넣어도 가볍다(데모 예측가·로딩 카운터가 바로 쓴다).
// 스크린리더에는 완성값만 읽히게 한다(스펙 §9.3): 완성값은 sr-only, 움직이는 글자는 aria-hidden.
export const FLIP = { charMs: 40, maxMs: 800 } as const;

const DIGITS = '0123456789';
const LETTERS = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';

// 글자마다 자리 잡는 시각(ms). 글자당 40ms씩 늦게, 글자가 많아 0.8초를 넘으면 간격을 줄인다
export function settleTimes(n: number): number[] {
  if (n <= 0) return [];
  const step = Math.min(FLIP.charMs, FLIP.maxMs / n);
  return Array.from({ length: n }, (_, i) => Math.round((i + 1) * step));
}

function scramble(c: string, rand: () => number): string {
  const pool = /[0-9]/.test(c) ? DIGITS : /[A-Z]/.test(c) ? LETTERS : null;
  return pool ? pool[Math.floor(rand() * pool.length)] : c;
}

export function frameAt(final: string, t: number, times: number[], rand: () => number): string {
  return [...final].map((c, i) => (t >= times[i] ? c : scramble(c, rand))).join('');
}

function reducedMotion(): boolean {
  try { return window.matchMedia('(prefers-reduced-motion: reduce)').matches; } catch { return true; }
}

// el의 내용을 final로 플립한다. 되돌리는 함수(애니메이션 취소)를 돌려준다.
export function flip(el: HTMLElement, final: string): () => void {
  const sr = document.createElement('span');
  sr.className = 'sr-only';
  sr.textContent = final;
  const vis = document.createElement('span');
  vis.setAttribute('aria-hidden', 'true');
  el.replaceChildren(sr, vis);
  if (reducedMotion()) {
    vis.textContent = final;
    return () => {};
  }
  const times = settleTimes([...final].length);
  const end = times.at(-1) ?? 0;
  const start = performance.now();
  let raf = 0;
  const tick = (now: number) => {
    const t = now - start;
    vis.textContent = frameAt(final, t, times, Math.random);
    if (t < end) raf = requestAnimationFrame(tick);
  };
  raf = requestAnimationFrame(tick);
  return () => { cancelAnimationFrame(raf); vis.textContent = final; };
}
```

Run: `npx vitest run tests/unit/flip.test.ts` / Expected: PASS

- [ ] **Step 3: 데모 예측가** — `src/components/demo/DemoResult.tsx`

React가 관리하는 글자와 플립이 서로 덮어쓰지 않도록, 예측가 칸은 React가 자식을 그리지 않는 빈 `span`으로 두고 효과(effect)에서 채운다. 처음 표시는 플립(데모를 열 때 한 번), 이후 값이 바뀔 때마다 플립.

```tsx
import { useEffect, useRef } from 'react';
import { flip } from '@/motion/flip';
```

컴포넌트 안:

```tsx
  const priceEl = useRef<HTMLSpanElement>(null);
  const priceText = money(price);
  useEffect(() => {
    // 예측가는 플립 글자판(스펙 §4). React는 이 칸의 자식을 그리지 않고 flip이 채운다
    if (priceEl.current) return flip(priceEl.current, priceText);
  }, [priceText]);
```

예측가 줄: `<span className="mono" data-flip>{money(price)}</span>` → `<span className="mono" data-flip ref={priceEl} />`

`DemoResult`가 훅을 쓰므로 파일이 클라이언트 경계 안(DemoApp에서만 import)인지 확인한다(서버 컴포넌트에서 import하면 안 된다).

- [ ] **Step 4: 확인**

Run: `npm run typecheck && npm test && npm run build && npx playwright test tests/e2e/demo.spec.ts`
Expected: PASS(데모 e2e는 예측가 글자를 직접 보지 않는다. 실패하면 선택자를 확인)

- [ ] **Step 5: 커밋**

```bash
git add src/motion/flip.ts tests/unit/flip.test.ts src/components/demo/DemoResult.tsx
git commit -m "feat: split-flap text module; flip the demo predicted price"
```

---

### Task 3: 로딩 화면 `LOADING 242,874 ROWS`

**Files:**
- Create: `src/components/Loader.tsx`
- Modify: `src/components/HomePage.tsx`, `src/styles/globals.css`

- [ ] **Step 1: `src/components/Loader.tsx`**

```tsx
'use client';
// 로딩 화면(스펙 §3-0, §8.1): 최대 1.2초의 고정 연출이며 실제 로딩과 무관하다. 서버 HTML에 들어 있고
// CSS 애니메이션만으로 사라지므로 JS가 늦거나 실패해도 화면을 막지 않는다. 숫자 플립만 여기서 얹는다.
// 같은 세션 재방문·움직임 줄이기·캡처 모드·JS 없음에서는 부트 스크립트/noscript가 숨긴다(src/lib/boot.ts).
import { useEffect, useRef } from 'react';
import { flip } from '@/motion/flip';

export function Loader({ rows }: { rows: string }) {
  const count = useRef<HTMLSpanElement>(null);
  useEffect(() => {
    if (!count.current || document.documentElement.classList.contains('no-loader')) return;
    return flip(count.current, rows);
  }, [rows]);
  return (
    <div className="loader" aria-hidden="true">
      <p>LOADING <span data-count ref={count}>{rows}</span> ROWS</p>
    </div>
  );
}
```

- [ ] **Step 2: `src/components/HomePage.tsx`**

import 추가: `import { Loader } from './Loader';`

`<Backdrop …/>` 앞에 넣는다. 숫자는 facts에서 영문 자리 구분으로(세 언어 공통 연출 글자):

```tsx
      <Loader rows={new Intl.NumberFormat('en-US').format(facts.data.filteredRows)} />
```

- [ ] **Step 3: CSS** — `src/styles/globals.css`의 `.sr-only` 줄 다음에 추가:

```css
/* 로딩 화면: 0.85초 머문 뒤 0.35초 동안 사라진다(합 1.2초, 스펙 §8.1). 클릭을 막지 않고 레이아웃도 밀지 않는다 */
.loader { position: fixed; inset: 0; z-index: 90; display: grid; place-items: center; background: var(--bg);
  pointer-events: none; animation: loader-out 0.35s var(--ease) 0.85s forwards; }
.loader p { font: 600 clamp(0.85rem, 2.4vw, 1.1rem) var(--font-mono); letter-spacing: 0.18em; color: var(--mute); }
.loader [data-count] { color: var(--amb); }
@keyframes loader-out { to { opacity: 0; visibility: hidden; } }
html.no-loader .loader { display: none; }
```

- [ ] **Step 4: 확인**

Run: `npm run typecheck && npm test && npm run build && npm run size && npm run e2e`
Expected: 모두 PASS. 로딩 화면이 1.2초 동안 덮여 있어 실패하는 e2e가 있으면(예: 즉시 스크린샷·화소 검사), 로딩 화면 때문인지 확인하고 그 테스트에서 로딩 화면이 사라진 뒤(`.loader`가 hidden) 진행하도록 기다림을 넣는다. 클릭이 막혀 실패한다면 `pointer-events: none`이 적용됐는지부터 본다.

브라우저로 한 번 본다: `npx serve out -l 4180`을 띄우고 Playwright 스크립트로 `/`를 열어 0.3초·0.9초·1.4초에 화면을 찍어 Read로 확인(카운터가 호박색으로 뒤섞이다 `242,874`에 자리 잡고, 1.2초 무렵 사라짐). 같은 페이지를 새로고침하면 로딩 화면이 없어야 한다.

- [ ] **Step 5: 커밋**

```bash
git add src/components/Loader.tsx src/components/HomePage.tsx src/styles/globals.css
git commit -m "feat: 1.2s loading screen with a flipping row counter"
```

---

### Task 4: 모션 기반 — Lenis + ScrollTrigger, 제목 리빌, GATE 플립

**Files:**
- Create: `src/motion/words.ts`, `tests/unit/words.test.ts`, `src/motion/run.ts`, `src/components/Motion.tsx`
- Modify: `package.json`, `src/components/HomePage.tsx`, `src/components/sections/{About,CaseStudy,Demo,Stack,Contact}.tsx`, `src/styles/globals.css`

- [ ] **Step 1: 설치**

```bash
npm install gsap@^3.15 lenis@^1.3
```

- [ ] **Step 2: 실패하는 테스트** — `tests/unit/words.test.ts`

```ts
// 리빌용 단어 나누기: 공백 기준으로 나누되 공백은 보존한다(다시 이어 붙이면 원문과 같다).
import { describe, expect, it } from 'vitest';
import { splitWords } from '@/motion/words';

describe('splitWords', () => {
  it('단어와 공백을 번갈아 보존', () => {
    expect(splitWords('한·일 항공권, 언제 사야 할까?')).toEqual(['한·일', ' ', '항공권,', ' ', '언제', ' ', '사야', ' ', '할까?']);
  });
  it('이어 붙이면 원문', () => {
    const s = '  Buy now  or wait? ';
    expect(splitWords(s).join('')).toBe(s);
  });
  it('공백 없는 일본어는 한 덩어리', () => expect(splitWords('今買う?')).toEqual(['今買う?']));
});
```

Run: `npx vitest run tests/unit/words.test.ts` / Expected: FAIL

- [ ] **Step 3: `src/motion/words.ts`**

```ts
// 텍스트 리빌(스펙 §4)용 단어 나누기. 공백을 따로 보존해 다시 이어 붙이면 원문과 같게 한다.
// 공백이 없는 일본어 제목은 한 덩어리로 떠오른다(글자 단위로 쪼개면 읽는 흐름이 끊겨 보인다).
export function splitWords(text: string): string[] {
  return text.split(/(\s+)/).filter((s) => s.length > 0);
}
```

Run: `npx vitest run tests/unit/words.test.ts` / Expected: PASS

- [ ] **Step 4: `src/motion/run.ts`**

```ts
// 스크롤 연출 시작/정리. GSAP·ScrollTrigger·Lenis를 이 파일에만 모아, Motion.tsx가 첫 그리기 뒤 import()로
// 불러온다(초기 JS 150KB 유지). 움직임 줄이기에서는 이 파일을 아예 불러오지 않는다.
// - Lenis: 부드러운 스크롤. 브라우저 기본 스크롤을 움직이므로 3D 카메라(scroll 이벤트)는 그대로 따라온다
// - [data-reveal] 제목: 단어 단위로 아래에서 떠오름(0.9초, 단어당 60ms, expo.out = cubic-bezier(.16,1,.3,1))
// - [data-flip-on-enter] GATE 제목: 화면에 들어오면 한 번 플립
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import Lenis from 'lenis';
import { flip } from './flip';
import { splitWords } from './words';

const ENTER = 'top 85%'; // 요소 윗변이 화면 85% 높이에 닿을 때 — 완전히 보이기 조금 전에 시작해 늦어 보이지 않게

function wrapWords(el: HTMLElement): HTMLElement[] {
  const inners: HTMLElement[] = [];
  const parts = splitWords(el.textContent ?? '');
  el.replaceChildren(...parts.map((part) => {
    if (/^\s+$/.test(part)) return document.createTextNode(part);
    const outer = document.createElement('span');
    outer.className = 'reveal-word';
    const inner = document.createElement('span');
    inner.textContent = part;
    outer.append(inner);
    inners.push(inner);
    return outer;
  }));
  return inners;
}

export function startMotion(doc: Document): () => void {
  gsap.registerPlugin(ScrollTrigger);
  const lenis = new Lenis({ autoRaf: false, anchors: true });
  const raf = (time: number) => lenis.raf(time * 1000);
  gsap.ticker.add(raf);
  gsap.ticker.lagSmoothing(0);
  lenis.on('scroll', ScrollTrigger.update);

  const cancels: (() => void)[] = [];
  const ctx = gsap.context(() => {
    doc.querySelectorAll<HTMLElement>('[data-reveal]').forEach((el) => {
      gsap.from(wrapWords(el), {
        yPercent: 110, opacity: 0, duration: 0.9, stagger: 0.06, ease: 'expo.out',
        scrollTrigger: { trigger: el, start: ENTER, once: true },
      });
    });
    doc.querySelectorAll<HTMLElement>('[data-flip-on-enter]').forEach((el) => {
      const text = el.textContent ?? '';
      ScrollTrigger.create({ trigger: el, start: ENTER, once: true, onEnter: () => cancels.push(flip(el, text)) });
    });
  });

  return () => {
    cancels.forEach((c) => c());
    ctx.revert();
    gsap.ticker.remove(raf);
    lenis.destroy();
  };
}
```

(`gsap.context().revert()`는 `from` 트윈을 되돌리지만 `wrapWords`로 바꾼 DOM은 되돌리지 않는다. 페이지를 떠날 때만 정리되므로 괜찮다.)

- [ ] **Step 5: `src/components/Motion.tsx`**

```tsx
'use client';
// 스크롤 연출(run.ts)을 첫 그리기 뒤 한가할 때 불러온다. 움직임 줄이기면 아무것도 불러오지 않는다.
import { useEffect } from 'react';

export function Motion() {
  useEffect(() => {
    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;
    let stop: (() => void) | undefined;
    let cancelled = false;
    const idle = window.requestIdleCallback ?? ((cb: () => void) => window.setTimeout(cb, 300));
    idle(() => {
      import('@/motion/run').then((m) => { if (!cancelled) stop = m.startMotion(document); }).catch((e) => console.warn('연출 없이 진행', e));
    });
    return () => { cancelled = true; stop?.(); };
  }, []);
  return null;
}
```

`src/components/HomePage.tsx`: import `import { Motion } from './Motion';`, `</main>` 다음에 `<Motion />`.

- [ ] **Step 6: 표식 달기**
- `About.tsx`, `CaseStudy.tsx`(`#case-h`), `Demo.tsx`, `Stack.tsx`, `Contact.tsx`의 `<h2 … className="display">`에 `data-reveal` 속성을 단다. **`Hero.tsx`의 `h1`에는 달지 않는다**(LCP).
- `CaseStudy.tsx`의 챕터 `<p className="eyebrow">{c.gate}</p>`에 `data-flip-on-enter`를 단다.

CSS(`globals.css` 끝):

```css
/* 텍스트 리빌: 단어가 자기 줄 아래에서 떠오르도록 바깥 상자에서 잘라 낸다 */
.reveal-word { display: inline-block; overflow: hidden; vertical-align: top; }
.reveal-word > span { display: inline-block; }
```

- [ ] **Step 7: 확인**

Run: `npx vitest run tests/unit/words.test.ts tests/unit/client-imports.test.ts && npm run typecheck && npm test && npm run build && npm run size`
Expected: PASS, 초기 JS 150KB 안(`gsap`·`lenis`는 `run.ts`를 통한 지연 청크에만). `grep -l "ScrollTrigger" out/_next/static/chunks/*.js`의 파일이 `out/index.html`에 없어야 한다.

Run: `npm run e2e` / Expected: PASS

- [ ] **Step 8: 커밋**

```bash
git add package.json package-lock.json src/motion src/components/Motion.tsx src/components/HomePage.tsx src/components/sections src/styles/globals.css tests/unit/words.test.ts
git commit -m "feat: Lenis smooth scroll, word reveal for headings, GATE titles flip on enter"
```

---

### Task 5: 휴대폰 3-5 예측 구간 띠 읽히게

**Files:**
- Modify: `src/three/data.ts`, `src/three/shaders.ts`, `tests/unit/three-data.test.ts`, `public/fallback/interval.webp`

**배경:** 먼 출발일은 지형 날짜가 주 1회라, 매일 있는 띠 날짜 여러 개가 같은 지형 칸 사이에 빽빽이 보간되어 겹친다. 더하기 합성(AdditiveBlending)으로 겹친 곳이 흰 덩어리가 된다. 지형 출발일과 정확히 맞는 날짜의 띠만 세우면(가까운 쪽은 매일, 먼 쪽은 주 1회 — 지형과 같은 간격) 겹침이 사라지고 띠가 지형 칸과 줄을 맞춘다.

- [ ] **Step 1: 테스트** — `tests/unit/three-data.test.ts`의 띠 describe 안에 추가:

```ts
  it('지형 출발일과 맞지 않는(사이에 끼는) 날짜의 띠는 세우지 않는다 — 먼 구간 겹침 방지', () => {
    // fixture 레이어가 출발일 번호 0~2를 쓰므로 날짜는 3개로 두고, 02일이 01일과 05일 사이에 끼게 한다
    const sparse: Terrain = { ...terrain, dates: ['2026-10-01', '2026-10-05', '2026-10-08'] };
    const between: Band = { asOf: '2026-09-22', dates: ['2026-10-02'], lo: [-100], hi: [100] };
    const plain = buildPointCloud(sparse, map, { noiseStride: 1 });
    expect(buildPointCloud(sparse, map, { noiseStride: 1, band: between }).count).toBe(plain.count);
  });
```

Run: `npx vitest run tests/unit/three-data.test.ts` / Expected: 새 테스트 FAIL

- [ ] **Step 2: 구현** — `src/three/data.ts`의 띠 루프에서 `if (di === null) return;` 다음 줄에 추가:

```ts
      // 지형 출발일 사이에 끼는 날짜는 건너뛴다. 먼 구간은 지형 날짜가 주 1회라, 매일 있는 띠를 사이에
      // 보간하면 좁은 z에 겹쳐 흰 덩어리가 된다(휴대폰 세로 화면에서 특히). 지형과 같은 간격으로만 세운다
      if (Math.abs(di - Math.round(di)) > 1e-6) return;
```

`src/three/shaders.ts` 크기 줄에서 kind 4의 `1.0` → `1.25`(듬성해진 만큼 점을 조금 키워 선이 끊겨 보이지 않게).

Run: `npx vitest run tests/unit/three-data.test.ts` / Expected: PASS(기존 띠 테스트의 `2026-10-02`는 fixture 지형 날짜와 정확히 맞아 그대로 통과)

- [ ] **Step 3: 캡처와 눈 확인**

```bash
npm run build && npm run fallbacks
```

`public/fallback/interval.webp`를 Read로 본다(흰 세로 띠가 지형 앞 가장자리를 따라 줄지어 선다). 세로 화면은 `npx serve out -l 4182` + Playwright(412×915, isMobile)로 `[data-chapter="interval"]`까지 스크롤해 4초 뒤 찍어 Read로 본다: 띠가 덩어리가 아니라 세로 줄들로 읽혀야 한다. 여전히 덩어리면 kind 4 알파(`0.85 * uBand`)를 0.6으로 낮춰 다시 본다(한 번만). 다른 네 장면 대체 이미지는 바뀌지 않아야 한다(`git diff --stat public/fallback`).

- [ ] **Step 4: 커밋**

```bash
git add src/three/data.ts src/three/shaders.ts tests/unit/three-data.test.ts public/fallback/interval.webp
git commit -m "fix: interval band only on terrain-aligned dates so it reads on phones"
```

---

### Task 6: 연출 e2e

**Files:**
- Create: `tests/e2e/motion.spec.ts`

- [ ] **Step 1: `tests/e2e/motion.spec.ts`**

```ts
// 연출 e2e: 로딩 화면(1.2초 뒤 사라짐, 재방문 생략, 움직임 줄이기 생략), 제목 리빌, GATE 플립의 완성값·낭독,
// 연출 라이브러리가 초기 청크에 없는지.
import { expect, test } from '@playwright/test';
import { readdirSync, readFileSync } from 'node:fs';

test('로딩 화면은 1.2초 안팎에 사라지고, 같은 세션 새로고침에는 없다', async ({ page }) => {
  await page.goto('/');
  const loader = page.locator('.loader');
  // 페이지를 받는 데 1.2초 넘게 걸리는 환경(CI)도 있어 "보인다"는 검사하지 않는다 — 처음 방문엔 생략되지 않았고, 곧 사라지는지만 본다
  await expect(page.locator('html')).not.toHaveClass(/no-loader/);
  await expect(loader).toBeHidden({ timeout: 3_000 });
  await expect(page.locator('.loader [data-count] .sr-only')).toHaveText('242,874');
  await page.reload();
  await expect(page.locator('html')).toHaveClass(/no-loader/);
  await expect(loader).toBeHidden();
});

test.describe('움직임 줄이기', () => {
  test.use({ reducedMotion: 'reduce' });
  test('로딩 화면·리빌·플립 없이 최종 글자가 바로 보인다', async ({ page }) => {
    await page.goto('/');
    await expect(page.locator('.loader')).toBeHidden();
    await page.locator('#stack').scrollIntoViewIfNeeded();
    await page.waitForTimeout(1500);
    await expect(page.locator('.reveal-word')).toHaveCount(0);
    await expect(page.locator('[data-chapter="problem"] .eyebrow')).toHaveText('GATE 01 — PROBLEM');
  });
});

test('제목은 화면에 들어오면 단어 단위로 떠올라 끝내 보인다', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('html')).toHaveAttribute('data-3d', /^(on|off)$/, { timeout: 20_000 });
  const h = page.locator('#stack-h');
  await h.scrollIntoViewIfNeeded();
  await expect(h.locator('.reveal-word').first()).toBeAttached({ timeout: 5_000 });
  await page.waitForTimeout(1500); // 0.9초 + 단어 지연
  await expect(h).toHaveText(await h.textContent() ?? '');
  const opacity = await h.locator('.reveal-word > span').last().evaluate((n) => getComputedStyle(n).opacity);
  expect(Number(opacity)).toBe(1);
});

test('GATE 제목은 플립 뒤 완성값이고, 스크린리더용 완성값이 따로 있다', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('html')).toHaveAttribute('data-3d', /^(on|off)$/, { timeout: 20_000 });
  const gate = page.locator('[data-chapter="bubble"] .eyebrow');
  await gate.scrollIntoViewIfNeeded();
  await expect(gate.locator('.sr-only')).toHaveText('GATE 03 — R² BUBBLE', { timeout: 5_000 });
  await expect(gate.locator('[aria-hidden="true"]')).toHaveText('GATE 03 — R² BUBBLE', { timeout: 2_000 });
});

test('연출 라이브러리(ScrollTrigger·Lenis)는 초기 HTML이 직접 불러오지 않는다', async () => {
  const dir = 'out/_next/static/chunks';
  const chunks = readdirSync(dir).filter((f) => f.endsWith('.js') && /ScrollTrigger|lenis/i.test(readFileSync(`${dir}/${f}`, 'utf8')));
  expect(chunks.length).toBeGreaterThan(0);
  for (const file of ['out/index.html', 'out/en/index.html', 'out/ja/index.html']) {
    const html = readFileSync(file, 'utf8');
    for (const f of chunks) expect(html, file).not.toContain(f);
  }
});
```

- [ ] **Step 2: 실행**

Run: `npm run build && npx playwright test tests/e2e/motion.spec.ts`
Expected: desktop·mobile PASS. 실패하면 `superpowers:systematic-debugging`으로 원인을 찾는다(예: 로딩 화면 판정이 테스트 사이 세션을 공유하는지 — Playwright는 테스트마다 새 컨텍스트라 공유하지 않는다).

Run: `npm run e2e` / Expected: 전체 PASS

- [ ] **Step 3: 커밋**

```bash
git add tests/e2e/motion.spec.ts
git commit -m "test: e2e for loading screen, reveal, flip and lazy motion chunk"
```

---

### Task 7: 문서, 전체 검증, PR, 사용자 확인

**Files:**
- Modify: `README.md`(필요 시), `CLAUDE.md`, `docs/superpowers/specs/2026-09-23-portfolio-design.md`

- [ ] **Step 1: 측정** — `npm run build && npm run lighthouse`로 로컬 전후 비교(Task 1 전 기록과), 결과를 적어 둔다.

- [ ] **Step 2: 스펙** — §8.1 로딩 순서에 "첫 그리기 전 부트 스크립트가 3D 판정 대기(`data-3d="pending"`)를 걸어 대체 이미지를 숨기고, 판정이 끝나면 on/off(최대 6초)"를 추가. §4 모션 규칙 아래에 구현 요약(플립 `src/motion/flip.ts` rAF, 리빌·Lenis·ScrollTrigger `src/motion/run.ts` 지연 로딩, 첫 화면 이름은 리빌 제외 — LCP)을 한 단락으로.

- [ ] **Step 3: CLAUDE.md** — 진행 상태 표에 `| 계획 4-2: 연출·성능 | ✅ 구현 2026-09-24 · branch \`plan-4-2-motion\` · PR, 사용자 연출 확인 후 병합 |`. "다음 세션 할 일"의 계획 4-2 항목들에 ✅. 남은 것: 공개 전 할 일(스펙 §14)과 공개 전환, 운영 Lighthouse 재측정(병합 뒤).

- [ ] **Step 4: 전체 검증**

```bash
npm run typecheck && npm test && npm run pytest && npm run build && npm run size && npm run e2e
```

Expected: 모두 성공.

- [ ] **Step 5: PR**

```bash
git add -A docs CLAUDE.md README.md
git commit -m "docs: record plan 4-2 motion and performance"
git push -u origin plan-4-2-motion
gh pr create --base main --head plan-4-2-motion --title "Plan 4-2: motion and performance" --body "$(cat <<'EOF'
## 요약
- 첫 그리기 전 3D 판정 대기: 첫 화면 대체 이미지를 LCP에서 빼고 받지 않음
- 로딩 화면 LOADING 242,874 ROWS(1.2초, CSS만으로 사라짐, 재방문·움직임 줄이기 생략)
- 플립 글자판(로딩 카운터, GATE 제목, 데모 예측가), 제목 단어 리빌, Lenis 부드러운 스크롤 — 연출 라이브러리는 지연 로딩
- 휴대폰에서 3-5 예측 구간 띠가 읽히도록 지형 출발일과 맞는 날만 세움

## 확인
- typecheck, vitest, pytest, build, size, Playwright(desktop·mobile, axe) 통과

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

`gh pr checks`로 CI 통과를 확인한다(Google Fonts 일시 오류면 실패 작업만 한 번 다시 돌린다). **병합하지 않는다** — 사용자가 Vercel 미리보기에서 로딩 화면·리빌·플립을 보고 승인한 뒤 병합한다. 병합 뒤 운영 주소로 Lighthouse를 다시 잰다.
